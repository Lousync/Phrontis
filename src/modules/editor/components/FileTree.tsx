/**
 * 文件树已上移全仓共享层 `src/components/shared/VaultTree.tsx`
 * （笔记合并 Phase 2 批次 1，docs/notes-merge-phase2-design.md §2）——
 * 这里 re-export 保持编辑器模块既有 import 与契约脚本引用不变。
 */
export { VaultTree as FileTree } from '../../../components/shared/VaultTree'
