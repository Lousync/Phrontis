# v3.4.0 体验反馈台账

> **用途**：v3.4.0 是窗口骨架大改版（三栏外壳 + 书架六格式 + 书市）。改版必然带来「以前能做的、现在做不了」与「以前顺手、现在别扭」的落差。本文件是**这轮体验反馈的唯一归集处** —— 开发负责人逐条报问题，这里记现象、定位、根因、处置方向，攒够一批再统一开修。
>
> **与其他文档的分界**：
> - 与 `docs/pending-fixes.md`（B-n）：那份是**通用**待修 bug 台账，滚动了 B-1…B-26。本文件只收 **v3.4.0 改版引发的落差**，是本轮的专项台账。**若某条确认是通用 bug（与改版无关），转登记进 `pending-fixes.md` 并从本表移除**。
> - 与 `CHANGELOG.md`：那份面向用户、只写已交付的功能。本文件是**开发侧**的待办输入。
> - 与 DP `Phrontis/更新计划/v3.5.0.md`：跨版本的排期项在那；本文件只管 v3.4.0 自身的修补。
>
> **编号**：`F-n`（Feedback），全局递增不复用。状态标记 `[ ]` 待办 / `[~]` 处理中 / `[x]` 已修 / `[-]` 不修（有意设计，保留说明）。
>
> **本文档收两类，分两个区段，前缀不同，勿混**：
> - **`F-n`（缺陷 / 落差）** —— 「以前能、现在不能」（改版落差）或「本来就不对」（bug）。**本区段**。
> - **`N-n`（New，功能需求）** —— 「从来没有、现在想要」（新增能力）。见文末 **「功能需求」区段**。二者各自维护状态总览表。
>
> **★ 关键前提：先判「是不是有意设计」。** v3.4.0 有一批能力是**按方案主动移除**的（如图标条拖拽排序，见 F-1）。这类先标 `[-]` 并给出方案依据，**等开发负责人拍板「确实要恢复」再转 `[ ]`** —— 避免把设计决策当 bug 修回去。

---

## 状态总览

| 编号 | 简述 | 性质 | 状态 |
|---|---|---|---|
| F-1 | 图标条不能拖拽排序、不能右键隐藏图标 | **已拍板恢复（D 选项）** | `[ ]` 待实现 |
| F-2 | 版本号显示为 `v3.4.0-beta.2`（未发布 release） | 记录 | — |
| F-3 | 托盘图标有时变成紫色方块 | **真 bug（图标加载失败走兜底）** | `[ ]` 待修 |
| F-4 | 更新说明点「知道了」开出空白 editor 标签页 | **已修** | `[x]` |
| F-5 | 书市首页副标题冗余（「预置的只有公版与公共目录」重复三处） | UI 精简 | `[x]` 已删 |
| F-6 | 书源标签说「无需登录」、同时又说「需要凭据」 | **两处缺陷均已修** | `[x]` |
| F-7 | 沉浸阅读状态下页面不能向下滚动 | **真 bug —— 根因已定位，须实机确认后修** | `[~]` |
| F-8 | 非激活标签页显示成「打开（发光）」状态 | **真 bug —— 根因已确证（激活态缺 App 层归零）** | `[~]` |
| F-9 | PDF 左侧目录跳转与目标页不匹配（差一页 / 首页点了没反应） | **真 bug —— 根因已确证（0 基页号当 1 基用）** | `[~]` |

---

## [ ] F-1 图标条不能拖拽移动图标位置、不能右键唤出隐藏菜单

**报障（2026-09-23，开发负责人原话）**：「现在的左侧 tab 栏不能拖动移动图标的位置和排序了，而且不能右键唤出菜单来进行图标的隐藏了」。

**现象**：图标条（最左窄列）的按钮位置固定，无法拖动换位；右键点击无菜单，无法隐藏某个图标。

**性质判定：这是 v3.4.0 的**有意设计**（非回归），但**开发负责人已拍板恢复**。**

**依据（代码注释白纸黑字）**：`src/components/shared/ActivityBar.tsx:13-14`

> **固定按钮**：AI 教学 / 回收站 / 插件市场 / 动态 / 设置——**不再提供拖拽排序与右键显隐**（旧 `activityBarOrder` / `activityBarHidden` 设置键随之废弃，见方案 §3.5）；

**旁证**：
- 按钮清单是硬编码常量 `RAIL_BUTTONS`（`ActivityBar.tsx:31`），非从 settings 读取。
- 旧设置键 `activityBarOrder` 仍留在 `src/lib/settings.ts:170`，但 `ActivityBar` 已不消费它（仅 `appModules.ts` 注释提到）。属**残留键**，可考虑清理或保留兼容。
- **方案原文已核对**（`Phrontis/方案与调研/workbench-shell-design.md`，DP 库；主仓无副本）：§3.4 第 77 行明写 **「图标条不再提供拖拽排序/显隐菜单（v15 定稿固定 5 项），旧 `activityBarOrder` / `activityBarHidden` 设置键废弃」**；§3.5 第 89 行把两键列入「废弃键」清单。**结论 = 原型 v15 定稿时的有意决策，非回归。**

**★ 一处需要澄清的能力转移**：同方案 §3.3 第 71 行，`{ hidden: string[], order: string[] }` 这套**显隐 + 排序**能力在 v3.4.0 里**给了左栏书签**（`workbenchLayout.bookmarksHidden` 等），图标条没有。也就是说「用户可自定义入口的顺序与显隐」这件事在新外壳里换了个载体 —— 从图标条搬到了左栏书签。**若开发负责人真正想要的是「能自定义图标条」，那与「用左栏书签自定义」是两种产品取向，需先明确要哪个。**

### ★ 处置决定：**D 选项 —— 拖拽排序 + 右键显隐，两者都恢复**（2026-09-23 开发负责人拍板）

**已核实的三项有利条件**（实现成本比预想低）：

1. **旧实现完整存在于 git 历史** —— 提交 **`89156e5c`**「活动栏拖拽排序 + 右键切换模块可见性 + 侧栏拖拽关闭」（2026-06-21）：`ActivityBar.tsx` +188 行，用的是 **HTML5 拖拽重排**，顺序持久化到 `activityBarOrder`；右键空白处弹菜单勾选显隐。**可整段参照取回**，不必从零设计。
2. **两个设置键都还在、无人消费** —— `src/lib/settings.ts:170-171` 的 `activityBarOrder` / `activityBarHidden` 仍在（`type: 'json'`，`ui: false`）。**恢复不需要新增键**；方案 §3.5 把它们列进「废弃键」清单是当时判断，现按 D 选项**撤回该废弃决定**。
3. **右键菜单有现成基建可复用** —— `src/lib/useAnchoredMenu.ts`（v3.4.0 为右栏 `⋯` 菜单新抽的 hook：实测 `offsetHeight` 定位 + 边界翻转 + 外部点击/Esc 关闭）。**不要重写菜单定位**。

**实现要点**：

- **数据源**：`RAIL_BUTTONS` 由硬编码常量改为「可排序集合 + 消费 `activityBarOrder` / `activityBarHidden`」；顺序归一化可复用 `src/lib/appModules.ts:117` 的「活动栏归一化顺序（含被隐藏的项）」。
- **拖拽**（铁律 9）：图标条在 `-webkit-user-drag: none` 的继承链上，**拖拽源必须挂 `.kb-draggable`**（定义在 `src/styles/index.css:171`）。旧实现用的是 HTML5 拖拽 —— 图标条是**窄列纵向排序**，需确认 HTML5 拖放在此场景可靠（若出现静默失败，按铁律 9 换 pointer events，参考日程拖拽的 `dragGuard` 250ms 兜底）。
- **动效**（铁律 13）：排序过程走既有令牌，**不另造过渡**；拖拽跟手不加动画。
- **右键菜单**：复用 `useAnchoredMenu`，菜单项 = 各模块勾选（`activityBarHidden`）。注意旧实现的口径「用户 / 设置不受影响」（这两个不被隐藏）。
- **契约**：`.AGENT/scripts/verify-workbench-shell.mjs` 有「图标条按钮集断言」（方案 §3.4 第 180 行提到）—— 改成动态集合后**该断言需同步改写**，否则会锁住旧的固定清单。
- **待确认的一个设计问题**：图标条与**左栏书签**若都能自定义顺序/显隐，两处是否需要一致性（书签已有 `workbenchLayout.bookmarksHidden`）。**倾向各自独立**（图标条 = 应用级模块入口，书签 = 工作台内导航），但需在实现时明确，避免用户困惑「为什么有两套隐藏」。

---

## [ ] F-3 托盘图标有时变成紫色方块

**报障（2026-09-23，开发负责人原话）**：「软件在左下角的后台的这个图标有时候显示正常有时候显示不正常」，附截图 = 一个**紫色方块**。

**根因：已定位。紫色方块就是代码里的兜底占位图 —— 托盘图标加载失败时才会出现。**

**证据链**：

1. **兜底图确认为紫色方块**：`electron/main/index.ts:224-227`，三个候选路径全失败时执行 `nativeImage.createFromDataURL('<内置 base64>')`，并 `console.warn('[Tray] All icon candidates failed to load -> using builtin placeholder')`。**我把这段 base64 解码成 PNG 渲染出来验证过，就是一个紫色方块 —— 与截图完全一致。**
2. **三个候选路径中，只有第一条在 dev 下可能成功**（`electron/main/index.ts:216-220`）：
   - ① `app.getAppPath()/build/icon.png` ← **唯一有效候选**
   - ② `process.resourcesPath/build/icon.png` ← dev 下 = `node_modules/electron/dist/resources/build/icon.png`，**实测不存在**
   - ③ `process.resourcesPath/icon.png` ← 同上，**实测不存在**
3. **所以问题必然出在候选 ①** —— 即 `app.getAppPath()` 的取值时机/结果变化。

**★ 「有时正常有时不正常」的原因 —— 已确证的部分与尚未确证的部分**

**已确证**：

- **紫色方块 = 兜底图**（见上，已解码验证）。
- **候选 ② ③ 在 dev 下恒失败**（实测路径不存在）→ **只有候选 ① 有效**。
- **不是「某个 worktree 缺图标」**：两仓 `build/icon.png` 均存在且 **md5 相同**（`83a370352475baddb1f80a68557f198b`）。
- **不是启动时序问题**：`createTray()` 位于 `app.whenReady().then()` 回调内（`index.ts:696` 起，`createTray()` 在 1065 行），**不是 ready 之前调用**。此前的时序假设已排除。

**尚未确证（需实机抓日志）**：候选 ① 为何会时成时败。目前**未能在本会话复现**（Electron 探针在本沙箱起不来，已尝试三种启动方式均无输出；`tmp/` 下亦无历史日志可查 —— 已于本轮清理）。

**给下次复现时的快速判据（一击定位，不必再推理）**：

> **dev 窗口里按 F12 打开 DevTools 看不到主进程日志，要看启动终端**。托盘失败时主进程会打印：
> ```
> [Tray] All icon candidates failed to load -> using builtin placeholder
> ```
> **见到这行 = 确证走了兜底**，接下来只需打印 `app.getAppPath()` 与 `process.cwd()` 的实际值即可定位。

**修复方向（按两种可能分岔，待判据确认后二选一）**：

- **若是路径问题**（最可能）→ 把 `createTray` 的候选路径改为不依赖 `getAppPath()` 的推导方式（如以 `__dirname` 为基准，或按 `app.isPackaged` 分支：打包走 `process.resourcesPath`、dev 走 `app.getAppPath()`），并保证 dev 下 `build/` 的相对位置稳定。
- **若是时序 / 竞态问题** → 把 `createTray()` 移到更晚（如首个窗口 `ready-to-show` 之后），或改为对图标路径的**延迟求值 + 重试**。

**附注（必须一起改）**：任务栏按钮图标用的是**同一路径**（`index.ts:272` 的 `icon: join(app.getAppPath(), 'build', 'icon.png')`，注释已写明「dev 下 electron.exe 无内置图标 → 白板按钮的根因」）。**修 F-3 时两处要同批改**，否则会出现「托盘修好了、任务栏还是白的」。

**另有一条低成本加固（与根因无关，但值得做）**：既然兜底图是个无意义的紫方块，**不如把它换成真正的应用图标**（哪怕是小尺寸 PNG 的 base64 内联），这样即使将来再走兜底，用户看到的也是正常图标而非怪异的紫方块 —— 至少不会误以为「图标坏了」。

---

## [x] F-4 更新说明点「知道了」开出空白 editor 标签页

**已修（2026-09-23）**。根因是**三处缺陷叠加**，三处同批修掉：

1. **`dismissReleaseNotes` 绕开了归一化函数**（`App.tsx:458`）—— 它原先写 `(s.startupTab as TabName) || 'blog'`，直接用**未迁移的原始设置值**。而同一文件 `App.tsx:425` 的启动路径**正确调用了** `resolveStartupTab(s.startupTab, s.activityBarHidden)`。**同一份数据、两条路径、一条归一化一条不归一化** —— 这是空白页的最近原因。
   - 修：改用 `resolveStartupTab(s.startupTab, s.activityBarHidden)`。该函数已含「不可启动 / 已隐藏 / 已退役 ⇒ 落 knowledge」的钝规则，`editor` 不在 `STARTABLE_MODULE_IDS` 里，自然被兜住。
2. **手动入口不记来源**（`App.tsx:693`）—— `tabBeforeNotes` 原先只在**自动弹出**路径（`:448`）赋值，手动打开时恒为 `null`，于是必定走「回落启动项」这条分支。
   - 修：手动入口同样写 `tabBeforeNotes.current`（与自动路径同口径，`releaseNotes` 自身除外）。
3. **`openTab` 无有效性守卫**（`App.tsx:315`）—— 实现只有 `setActiveTab(tab)`，任何脏 TabName 都会直达渲染层，`switch` 无对应 case 即空白。
   - 修：`setActiveTab(isTabName(tab) ? tab : resolveStartupTab(undefined, s.activityBarHidden))`。**这是根治** —— 命令面板 / 设置 / 插件命令 / 深链 / 旧持久化值都走 `openTab`，此后一律不会再产生空白页。

**验证**：`tsc --web` 0 错；`verify-startup-tab.mjs` **53 项全绿**；`verify-workbench-shell.mjs` **139 项全绿**。

**遗留的同类风险（未修，建议后续排查）**：缺陷 1 的形态是「**同一个设置值有两条消费路径，一条归一化、一条没有**」。这类「归一化函数有、但某个调用点绕开它」的问题可能不止这一处 —— 建议全仓 grep `as TabName` 的**强制断言**，逐个确认是否该改走 `isTabName` / `resolveStartupTab`。（本次修前 `App.tsx` 里只有 `:461` 一处此类断言，已清。）

**原始根因分析（保留作过程记录）**

**缺陷 1 —— `dismissReleaseNotes` 不关当前页，而是另开一个**（`src/App.tsx:458-462`）：

```js
const dismissReleaseNotes = useCallback(() => {
  const prev = tabBeforeNotes.current
  tabBeforeNotes.current = null
  openTab(prev ?? ((s.startupTab as TabName) || 'blog'))
}, [openTab, s.startupTab])
```

它调 `openTab(prev)` —— **只切 activeTab，从不关闭 `releaseNotes` 标签**。

**缺陷 2 —— `tabBeforeNotes` 只在「自动弹出」路径被赋值**（`App.tsx:448`），手动打开更新说明时不赋值：

```js
tabBeforeNotes.current = prev === 'releaseNotes' ? null : prev   // 仅在 shouldAutoOpen 的定时器里
```

故**手动打开**时 `tabBeforeNotes.current === null` → 回落到 `s.startupTab || 'blog'`。

**缺陷 3（空白页的直接原因）—— `startupTab` 中存着已退役的 `'editor'`，且 `openTab` 不做校验**：

- `editor` 模块在 v3.4.0 **已整体退役、从 `APP_MODULES` 移除**（能力并入笔记区；`grep "'editor'" src/lib/appModules.ts` 零命中）。
- 但 `startupTab` 是**全局持久化设置**（`src/lib/settings.ts:178`，`scope: 'global'`），**用户旧设置里存的 `'editor'` 不会被自动迁移**。
- `openTab` 实现极简（`App.tsx:315`），**没有任何有效性校验**：

```js
const openTab = useCallback((tab: TabName) => {
  setActiveTab(tab); setSidebarOpen(true); setPalette(null)
}, [])
```

→ `activeTab = 'editor'`，渲染层 `switch(activeTab)` 无对应 case → 返回空 → **空白页**。

**为何标签页标题显示 `editor`**：标签标题取自 `TabName` 字符串本身（模块 label 查表失败时回落为 id）。

**关联**：缺陷 3 的「退役模块 id 残留在持久化设置里」是**一类问题**，不止 `startupTab` —— 建议顺手全仓排查其他可能出现旧 TabName 的持久化点（如 `workbenchLayout` 相关键、`openTabs` 的持久化若有）。

---

## [x] F-5 书市首页副标题冗余，已删除

**报障（2026-09-23，开发负责人原话）**：「关于书市的 UI 我发现有些提示太冗余难看了」，附截图 = 书市首页标题「书市」正下方的一行浅灰小字。

**截图中的原文**：

> 从书源里找到一本书，直接下载到书架。预置的只有公版与公共目录。

**位置（主处，即截图所示）**：`src/modules/bookmarket/DiscoverView.tsx:96-100`

```jsx
<div className="px-4 pb-5 pt-8 text-center">
  <h1 className="m-0 text-[22px] font-medium tracking-[.6px]">书市</h1>
  <p className="mx-auto mt-2.5 max-w-[520px] text-[12.5px] leading-[1.75] text-[var(--text-muted)]">
    从书源里找到一本书，直接下载到书架。预置的只有公版与公共目录。
  </p>
</div>
```

**★ 冗余的实质：「预置的只有公版与公共目录」这句话在书市里出现了三次** —— 这才是「冗余」观感的来源（同一信息反复出现）：

| # | 位置 | 原文 |
|---|---|---|
| 1 | `DiscoverView.tsx:99` | 从书源里找到一本书，直接下载到书架。**预置的只有公版与公共目录**。（← 截图这条） |
| 2 | `DiscoverView.tsx:154` | 将从 N 个已启用的书源里检索；**预置的只有公版与公共目录**。（检索前空态提示） |
| 3 | `SourcesView.tsx:61` | 书源 · **预置的只有公版与公共目录**（书源页小标题） |

**处置决定（2026-09-23 开发负责人拍板）：只删副标题 `<p>`，保留「书市」大标题；另两处（#2 / #3）不动。**

**执行清单**：
- 删 `DiscoverView.tsx:98-100` 的 `<p className="mx-auto mt-2.5 max-w-[520px] ...">从书源里找到一本书，直接下载到书架。预置的只有公版与公共目录。</p>`
- **保留** `<h1>书市</h1>` 与其外层 `<div className="px-4 pb-5 pt-8 text-center">`
- **#2（`DiscoverView.tsx:154`）保留** —— 它是「检索前空态」的状态说明，配随搜索框下方的空态图标，有信息价值
- **#3（`SourcesView.tsx:61`）保留** —— 书源页的身份小标题
- **删后检查留白**：原 `pt-8` 在 `<div>` 上，删 `<p>` 不影响 `pt-8`；但失去副标题后标题与搜索框的间距会变小，需目视确认是否过挤（`<p>` 带着 `mt-2.5`，删掉后总高度减约 22px）。

---

## [x] F-6 书源标签说「无需登录」，同时又说「需要凭据」

**报障（2026-09-23，开发负责人原话）**：「我让内置的 AI 助手给我配置书源，配置的是古登堡中文书源，配置的标签显示的是无需登录，但是又说需要凭证，究竟是 bug 还是 AI 助手幻觉了」。

**结论：不是 AI 幻觉，是两处设计缺陷叠加。** 下面分开说。

### 缺陷 1 —— AI 工具 schema 的表达盲区（`authType` 枚举缺 `none`）　★ **已修（2026-09-23）**

**位置**：`electron/lib/builtinTools.ts:2000`（工具 `builtin.booksource.draft` 的 inputSchema）

**修法**：enum 由 `['basic', 'bearer']` 补为 `['none', 'basic', 'bearer']`，description 同步写清「免登录的源填 none（或省略）」。

```js
authType: { type: 'string', enum: ['none', 'basic', 'bearer'], description: '免登录的源填 none（或省略）；仅确实要登录时才填 basic / bearer，凭据由用户手输' },
```

**一处必须记住的约束（铁律 16：单工具 schema ≤800 字符）**：首次补 enum 时 description 写得太长，schema 从 795 撑到 **806、超线**。压缩 description 后回到 **790**，合规。**改这个工具的 schema 前先看长度余量，只有 10 字符。**

**已补契约（`verify-book-market-tools.mjs` §②b）**：新增三条断言 —— 执行体 `bookSourceEnum(args.X, [...])` 的取值全集必须**逐个出现在** schema 的 enum 里，覆盖 `authType` / `kind` / `responseType`。
- **这条断言才是本 bug 的真正防线**：原契约完全没覆盖 `authType`，所以「schema 与执行体取值不一致」这类缺陷能一路溜到用户面前。
- **已做变异测试**：把 `none` 从 enum 拿掉 → 断言立即变红并准确报「缺 none」；还原后回绿。证明不是「永远绿」的空断言。

**原始分析（保留）**：schema 只声明 `basic` / `bearer`，而执行体（`:2018`）用 `bookSourceEnum(args.authType, ['basic', 'bearer', 'none'], 'none', 'authType')` —— 期望 `none` 表示「不需要认证」。⇒ 模型想说「此源免认证」时**契约里没有合法取值**，只能省略（正确，但 description 没说）或猜。**这是给模型的契约缺项，不是模型幻觉。**

### 缺陷 2 —— 「无需登录」与「需要凭据」是两个不同维度的信息，被并排渲染成矛盾　★ **已修（2026-09-23，彻底改法）**

**位置**：`src/modules/bookmarket/SourcesView.tsx:80-87`

```jsx
{s.authType ? (
  <span ...>{s.hasCredential ? '凭据已存' : '需要凭据'}</span>   // ← 静态标签：由 authType 决定
) : (
  <span className={tagCls}>无需登录</span>
)}
```

而**同一行右侧**还有一枚**连通性状态**（`CONN_META`，`shared.ts:44`）：

```js
'need-credential': { dot: 'var(--warning)', label: '需要凭据' },
```

这枚来自**实测探测结果**（`sourceClient.ts:149`）：

```js
if (status === 401 || status === 403) return 'need-credential'
```

⇒ **左边「无需登录」= 配置声明（`authType == null`），右边「需要凭据」= 实际探测到 401/403**。两者语义不同、维度不同（声明 vs 实测），但 UI 把它们摆在**同一行**、且**用了同一个词「需要凭据」**，于是看起来自相矛盾。

**为什么古登堡中文源会探测出 401/403**：这不是配置错误，而是**该端点真实的行为**。Gutenberg 的部分检索端点在特定查询下（或对中文检索、无 UA、频率限制时）会返回 401/403，`sourceClient` 据此判 `need-credential`。**它其实不是「要凭据」，是「被拒了」** —— 但这两种情况被合并成了同一个状态值。

### 修复方向（两处独立，可分开做）

**修缺陷 1（低风险，建议先做）**：
- `builtin.booksource.draft` 的 `authType` enum 补 `'none'`，并在 description 里写明**「公版 / 公共目录源请显式传 none，或直接省略该字段」**。让「不需要认证」有**可表达的取值**，模型就不会猜。
- 同类检查：`bookSourceEnum` 的默认参数是 `'none'`，说明执行体本就支持 —— 只是 schema 没暴露。

**★ 修缺陷 2 —— 采用「彻底改法」：`SourceConnectivity` 由三态拆为四态**（2026-09-23 开发负责人拍板）

**新状态设计**（拍板口径：401 与 403 语义不同，拆开）：

| 状态 | 来源 | 文案 | 用户该做什么 |
|---|---|---|---|
| `need-credential` | ① 配置缺凭据（`source.auth` 有值但凭据不全）② 服务端 **401** | 需要凭据 | 去填凭据 —— 填了就能好 |
| **`forbidden`（新）** | 服务端 **403** | **访问被拒** | 填凭据多半也没用 —— 换源 / 换地址 |

**为什么 401 不跟着进 `forbidden`**：401 = 「你没给身份」（填凭据可解），403 = 「你的身份不够 / 就是不放你进来」（常见于屏蔽 UA、地域限制、端点不对第三方开放）。**前者有解、后者通常无解**，混在一起会让用户白折腾。

**改动清单（7 处，跨主进程与渲染层）**：

| # | 文件 | 改动 |
|---|---|---|
| 1 | `electron/lib/bookMarket/sourceClient.ts:59` | 类型加 `'forbidden'` |
| 2 | 同上 `:63` | 新增 `REASON_FORBIDDEN = '访问被拒'` |
| 3 | 同上 `:159` | `classifyStatus` 拆行：401→need-credential、403→forbidden |
| 4 | 同上 `:166` | 新增 `reasonOf()` 收敛文案（原先三元内联，加态后不够用） |
| 5 | `src/types/index.ts:2116` | `BookSourceConnectivity` 加 `'forbidden'`（镜像主进程） |
| 6 | `src/modules/bookmarket/shared.ts:46` | `CONN_META` 加 `forbidden` 条目（「访问被拒」） |
| 7 | `index.tsx:259` + `SourceFormSheet.tsx:165` | **两处三元 toast 判断补 case** —— 漏了会把 forbidden 静默显示成「连接失败」 |

**顺手更新的注释**：`sourceClient.ts` 里 5 处「三态」措辞改为「连通性」/「四态」（`:55` 的 F-6 沿革说明刻意保留「三态」二字作历史记录）。

**验证**：

- **新建契约 `verify-source-connectivity.mjs`（26 项断言）**，六段：① 主进程/渲染层两侧四态同步 · ② `classifyStatus` 映射（含**负向断言：401/403 不许写在同一个 `if` 里**）· ③ 文案不撞词 · ④ `CONN_META` 有 forbidden 条目 · ⑤ 两处 UI 三元补了 case · ⑥ 运行期判据在场。
- **变异测试已做**：把 `classifyStatus` 改回 `if (status === 401 || status === 403)` → 两条断言立即变红（含那条专门的负向断言）；还原后回绿。
- `probe-s2-search.cjs` 加 `/opds/403` mock 路由 + §③b 段 4 条运行期断言（403→forbidden / 文案 / 不重试 / **源本身 auth=null 仍判 forbidden**，证明该态来自服务端而非配置缺口）。
- tsc 双端 0 错；书市 4 个契约脚本全绿。

**★ 未能完成的一项（环境限制，须留意）**：`probe-s2-search.cjs` 的**运行期断言未实际执行** —— Electron 在本会话沙箱内完全起不来（连 `electron --version` 都无输出，托盘探针亦同因被阻）。该探针需裸 electron，属**主进程探针**形态。**其运行期判据目前只有静态契约「在场性」断言保证（§⑥），未验过真实执行结果** —— 下次在正常环境跑一遍 `node .AGENT/scripts/book-market/probe-s2-search.cjs`（跑法见该文件头注，需先 esbuild 打入口）。

**原始分析（保留）**：
- **最小改法**（未采用）：只把探测侧文案改叫「访问被拒」，不拆状态 —— 改动小但底层仍是同一个值，探针/契约不受影响。
- **彻底改法**（已采用）：拆成两个状态，用户动作分得清。影响类型定义与多处消费点（评估后为 7 处，可控）。

**附：一个可复现的判据**（供修后验证）：配一条 `auth: null` 的源 → 点「测试」→ 若返回 401/403，修复前会同时显示「无需登录」+「需要凭据」；修复后应显示「无需登录」+「访问被拒」（或按拆分后的新状态）。

---

## [~] F-7 沉浸阅读状态下页面不能向下滚动

**报障（2026-09-23，开发负责人原话）**：「页面在沉浸式阅读状态下，页面不能向下滑动了」。

**★ 结论：根因已定位到「滚动根被变换劫持」，但复刻探针未能在 Chromium 里复现 —— 此项**须在实机确认一次再动手**，不可仅凭代码推理就改。**

**已确证的架构事实**（读码 + 真机 devbridge 量测，非推理）：

1. **沉浸阅读的滚动容器是 `src/modules/knowledge/index.tsx:1784`** 的 `<div className="h-full overflow-y-auto">`。
2. **该容器的祖先链上没有 `overflow:hidden` 把它逼成滚动根**——`ImportZone`（`:1754`）是 `relative h-full`，无 overflow。所以「内层不滚、外层裁剪」这一经典病灶在本分支**不成立**。
3. **孤立复刻探针：滚动正常**。用 Chromium（`tmp/probe-scroll-fillboth.mjs`）搭同构 4 层链（`.kb-view-fade` 动画 → `h-full overflow-hidden` → `h-full overflow-y-auto` → 3000px 内容），`outer` 即便带 `animation: … both`（计算 `transform` 仍为 `none`），`inner.scrollTop` 也能正常到 500。**⇒ 「`animation-fill-mode: both` 制造 containing block 劫持滚动根」这一候选**已证伪**（case ① 复刻通过；仅 case ④「inner 去掉 overflow-y:auto」失败，符合预期）。**

**⇒ 因此故障**不在**这条静态布局链上，而在「运行期该分支被换掉 / 高度约束断裂」这一类**（铁律 11 的 `h-full` vs `flex-1` 同族病）。**下次实机必查的三处**：

- **入口在跑吗**：进沉浸阅读后，量 `document.querySelector('div.h-full.overflow-y-auto')` 的 `clientHeight` 与 `scrollHeight`。若 `clientHeight === scrollHeight` ⇒ 容器没拿到高度（高度约束断了，内容高度没超过容器）；若 `clientHeight` 正常但 `scrollTop` 推不动 ⇒ 被祖先变换/paint 容器劫持。
- **谁是滚动根**：`document.scrollingElement`，以及沿祖先链逐个量 `overflow` / `transform` / `filter` / `will-change` / `contain`——任一非 `none` 的 `transform` 都会把 `position:fixed` 的包含块从视口改到该元素（本模块顶部「退出阅读」悬停区是 `absolute`，不受影响；但若别处有 `fixed` 就会错位）。
- **是不是高度约束**：查从 `App.tsx` 的那句 `min-h-0`（`:1445`，注释已写明这是**同类病的历史修复**）到模块根，是否有一条链漏了 `min-h-0` / `h-full`。**铁律 11 与 `docs/release-notes-design.md` §4 是同一类病**：`min-height:auto` 让 flex item 被内容撑破 → 外层 `overflow-hidden` 裁掉 → 「看得见上半屏、滚轮不动」。

**一处顺带发现（与本 bug 未必相关，但同属该函数）**：`readingPage` 是 `enterReading` 时的一次性快照（`:551-566`），仅由 `openInReading`（`:573`）/`openPageInReader`（`:599`）刷新，**阅读期间不随正文变化重取**。故「边读边被外部（AI 工具 / 另一窗口）改了该页」时，沉浸阅读显示的是旧正文——`MarkdownPreview` 拿的是 `readingPage.contentMd`（`:1820`）。**这不是「不能滚动」的原因**（旧内容一样可滚），但同属该函数的行为缺陷，修 F-7 时顺手确认是否需要「阅读态正文跟随刷新」。

**探针留档**：`tmp/probe-scroll-fillboth.mjs`（playwright-core + `%LOCALAPPDATA%\ms-playwright\chromium-1234`）——**否定证据**，勿删，修此 bug 时先跑它确认结论仍成立。

---

## [~] F-8 非激活标签页显示成「打开（发光）」状态

**报障（2026-09-23，开发负责人原话）**：「我在另一个标签页上，但是其他的两个零散页面的发光状态看起来就是打开的状态」（附截图：页面条上 `书架`/`日程`/`软件工程`/`接口技术` 四条，后两条「零散页面」条目看着像打开）。

**★ 根因已用真机量测确证（devbridge `ui.eval`）：知识库页签组的激活态**只看「哪个页面是当前页」，不看「知识库模块是不是当前标签**」。

**证据链（真实 DOM，非推理）**：

- 量得 `[data-pb-item]` 上：`data-pb-owner="knowledge"`、`data-tab-id=<页id>`、`class` 含 `kb-edge-tab-active`（= 发光样式），**但 `data-wb-active` 属性为 `null`**。
- 对照：`WorkbenchPageBar.tsx:176` 与 `:214` 对**每个**条目都写 `data-wb-active={isActive ? '1' : '0'}`；`PageTabStrip.tsx` **一处都不写**（`grep data-wb-active` 零命中）。**⇒ 两个渲染器对同一「激活」概念给出了不同的属性契约**，`PageTabStrip` 缺 `data-wb-active`。

**但「发光」的直接来源是 CSS 类，不是那个属性**：

- 发光 = `.kb-edge-tab-active`（`styles/index.css:1340-1351`，含 `::before/::after` 圆角缺口绘制），由 `PageTabStrip.tsx:152` 依 **`it.id === activeId`** 施加。
- 而 `activeId` 由知识库模块给出：`index.tsx:1857` → `activeId={showQuizCollection ? null : activePageId}`。
- **这里就是缺陷**：`activePageId` 是**模块内部**概念（「当前打开的是哪一页」），**不表达**「知识库是不是前台标签**」。`showQuizCollection` 已做了一层归零（2026-09-20「双激活」修复），**但没有归零「模块本身不在前台」这一情形**。
- ⇒ 用户切到别的标签（如 `书架`/`日程`）后，**知识库页签组仍按 `activePageId` 高亮它那一页** —— 于是「人在别的标签页，知识库的零散页面却仍发光」。截图里两条一起发光，是因为 `openPageIds` 里有多页、且 App 层 `activeTab` 与 `activePageId` 的「谁是真激活」两套状态没有单一收口。

**修复方向（两处，建议同批）**：

1. **`PageTabStrip` 补 `data-wb-active`**（`:136` 附近，与 `WorkbenchPageBar` 同口径 `isActive ? '1' : '0'`）。**这是契约补齐**——否则任何靠该属性判激活的 CSS / 探针 / 契约脚本在知识库页签组上恒失效（现在就是这样）。
2. **知识库页签组的激活态并入「模块是否前台」**：`activeId` 应为 `(知识库不在前台 || showQuizCollection) ? null : activePageId`。判据来源：模块已有的 `isActive` prop（App 传入 `on = t === activeTab`）——**别新造通道**。
3. **同批排查其它「模块内部激活」渲染面**：本次暴露的是「模块内维持着自己的『当前项』，切走标签后没有归零」。`WorkbenchLeftPanel.tsx:368` 的 `isActive` 已带 `!!activeTab` 判据（可作正确范例），`BookshelfSideList.tsx:115` 建议一并核对。

**契约缺口（须同批补）**：`.AGENT/scripts/workbench-shell/verify-workbench-shell.mjs` 目前锁的是 `kb-edge-tab-active` 类名，**没有锁 `data-wb-active` 属性在所有页面条渲染器上的在场性**——所以这个「两个渲染器契约不一致」能一路溜过契约全绿。

---

## [~] F-9 PDF 左侧目录跳转与目标页不匹配

**报障（2026-09-23，开发负责人原话）**：「我打开一本 pdf，他左侧的目录跳转跟实际的跳转页面不匹配」。

**★ 根因已确证：目录 dest 的页号是 **0 基**，却被当成 **1 基**页码使用 —— 整体差一页。**

**代码链**：

1. `src/components/shared/pdf/PdfOutlineTree.tsx:16-25` `destToPageNum`：解析 dest 数组首元素 `{num,gen}` 并 `return d[0].num`。
2. `PdfRailPanel.tsx:133-137` `jumpOutline`：`const p = await destToPageNum(...)`，随后 **`if (p >= 1) goto(p)`**。
3. `goto`（`:128-131`）派发 `KB_PDF_GOTO_PAGE` → `PdfReaderView.tsx:1194` `goPage(d.page)` → `:816` `Math.max(1, Math.min(Math.round(num), total))` → `:817` `scrollPageIntoView({ pageNumber: target })`。

**判据（pdf.js 官方 typing，`node_modules/pdfjs-dist/types/src/display/api.d.ts:823-827`）**：

> `getPageIndex(ref: RefProxy): Promise<number>` — "A promise that is resolved with the page index, **starting from zero**, that is associated with the reference."

dest 数组首元素**就是** `RefProxy`，故 `.num` 是 **0 基页索引**。而下游 `goPage` 全程按 **1 基**（`Math.max(1, ...)`、`pageNumber`）——**契约在最上游这一处就错了**。

**两个用户可见症状（都对应得上「不匹配」）**：

- **差一页**：绝大多数条目跳到**上一页**。
- **点了没反应**：dest 指向第 1 页（`num: 0`）时 `p === 0`，**过不了 `if (p >= 1)` 守卫 → 静默无跳转**（无 toast、无报错）。

**修复方向**：`destToPageNum` 改为**返回 1 基**。正确写法不是 `+1` 了事，而是走 pdf.js 的**权威换算**：

```ts
// 首元素是 RefProxy ⇒ 用 getPageIndex 拿 0 基索引再 +1。
// ★ 不要退回「按 .num + 1」：.num 是 PDF 对象号（obj num），
//   对页号不连续 / 有非页对象的 PDF 会算错；getPageIndex 才是「这份文档里它是第几页」的唯一真相源。
const idx = await pdf.getPageIndex(ref)   // 0 基
return idx + 1
```

**须同批处理的连带项**：

- `PdfRailPanel.tsx:136` 的 `if (p >= 1)` 守卫——改成 1 基后它依然成立，但**现在它是错误契约的残留物**；改完建议复核是否还需（防的是「解析失败回 1」，逻辑见下条）。
- `destToPageNum` 的**失败兜底 `return 1`**（`:23-24` 的 `catch`）在改成 1 基后语义正好一致（失败 → 第 1 页），**保留**。
- **缩略图 / 书签跳页不经此函数**（它们本来就按 1 基页码存），**不要一起改**——只动 `destToPageNum` 这一处，正是「改动收敛在唯一缺陷点」的体现。

**契约缺口（须同批补）**：`.AGENT/scripts/pdf-reader/` 下现有契约锁的是「事件链在场」（`verify-excerpt-export.mjs:206` 断言 `KB_PDF_GOTO_PAGE` 被派发/监听），**没有任何断言锁「dest→页号的基数是 1 基」**。建议新增一条负向断言：`destToPageNum` 的结果不得直接等于 `getPageIndex()` 的返回值（即必须 `+1`），并对「`num: 0` 的 dest 必须能跳」补一个用例。

---

## 记录性条目（非待修）

### F-2 版本号与发布状态（2026-09-23 备注）

- `package.json` 版本 = `3.4.0-beta.2`，安装包 `Phrontis-3.4.0-beta.2-setup.exe`（`dist-electron/`，**未发布 release**）。
- 线上最新 release 仍是 `v3.1.2`；`v3.2.0-beta.1` 与 `v3.3.0` 均未发布过（v3.3.0 整版跳过，其唯一项「仓库 git 集成」未开工、归属已改 v3.5.0）。
- 故本轮体验的基线是 **v3.1.2 → v3.4.0-beta.2 的全部变化**，见 `CHANGELOG.md` 的 v3.4.0 条目。

---

# 功能需求（新增能力，非缺陷）

> **本区段与上面的 `F-n` 区段严格分开，勿混：**
> - **`F-n`** = 「以前能、现在不能」（改版落差）或「本来就不对」（bug）—— 修的是**已有能力**。
> - **`N-n`** = 「从来没有、现在想要」（新增能力）—— 加的是**新东西**。
>
> 判定口诀：**「这功能以前有过吗？」** 有过且现在不行 → 进 `F-n`；从来没有 → 进 `N-n`。
> 二者**同一文档、不同区段、不同前缀**，各自维护自己的状态总览表。
>
> **编号**：`N-n`（N for New），全局递增不复用。状态标记 `[ ]` 待办 / `[~]` 处理中 / `[x]` 已做 / `[-]` 不做。
>
> **⚠️ 新需求进来先过一遍「是不是已评估暂缓」**：`AGENTS.md` 列了两项长期搁置（外部软件联动、语音输入 ASR），设计稿在 DP `Phrontis/搁置功能与想法/`；另有库查询（2026-09-23 判「暂不需要」）。**别把搁置项当新需求重记。**

## 状态总览（功能需求）

| 编号 | 简述 | 性质 | 状态 |
|---|---|---|---|
| N-1 | 收藏书签后左栏自动切到「书签」区（否则「点了没反应」） | 交互优化（PDF 左栏） | `[x]` 已做 |
| N-2 | 书签备注输入框改为「默认只读、点备注才编辑」（防误改） | 交互优化（PDF 左栏） | `[x]` 已做 |
| N-3 | 恢复「软件文件」入口 —— 编辑区并入知识库后，总览态丢了这个区 | **改版落差（能力丢失）** | `[~]` 四个待拍板点已定，待开修 |
| N-4 | AI 教学「编辑画像 / 要求」跳转后无返回入口（跳转本身也落错 Tab） | **改版落差（能力丢失）** | `[~]` 根因已确证，待开修 |
| N-5 | AI 助手独立的「要求 + 画像」（存放于软件文件区）+ 面板常驻编辑入口 | **新增能力** | `[~]` 已定 6 项，4 项待定，待开修 |

**★ 本轮「编辑区退役」造成的落差是一族，不止一条** —— `N-3`（软件文件区）、`N-4`（跳编辑区 + 返回）**同源**：都是**编辑区模块整体退役、能力并进知识库**时，**只搬了「就地编辑」本身，没搬围绕它的入口与回跳**。`N-5` 是同一块拼图上要**新增**的一角（助手侧的要求 + 画像）。**修的时候建议同批处理**，并顺带把「还有哪些编辑区入口没搬」全仓盘一遍（见 N-4 §五）。

**★ 本轮「编辑区退役」造成的落差是一族，不止一条**：`N-3`（软件文件区）、`N-4`（跳编辑区 + 返回）**同源** —— 都是**编辑区模块整体退役、能力并进知识库**时，**只搬了「就地编辑」本身，没搬围绕它的入口与回跳**。**修的时候建议同批处理**，并顺带把「还有哪些编辑区入口没搬」全仓盘一遍（见 N-4 §五）。

---

## [x] N-1 收藏书签后左栏自动切到「书签」区

**提出（2026-09-23，开发负责人原话）**：「我在阅读器中看一本书 PDF 的时候，我要设置一个书签，但是我的左侧边栏是在目录，导致我点击设置书签后用户的第一体验是没有反应，其实是有反应的只是左侧边栏没有自动跳转到书签状态（这个添加上）」。

**要解决什么**：左栏面板三区（目录 / 缩略图 / 书签）的切换状态 `section` 是 `PdfRailPanel` 的**组件内部 state**，而「收藏本页书签」按钮在 `PdfReaderView` 工具栏 —— **两者互不知情**。用户在「目录」区点收藏，书签列表其实已经加上了，只是左栏还停在目录 ⇒ 第一体验「点了没反应」。

**口径（已拍板）**：**只在「新增」书签时切**，移除时不切 —— 移除是「不要了」的语义，把用户强行拽到书签区反而多余。

**改动**：

| # | 文件 | 改动 |
|---|---|---|
| 1 | `src/components/shared/pdf/pdfEvents.ts` | 新增事件常量 `KB_BOOKMARK_SECTION_SHOW`（常量放此处，铁律 20：不拖 pdfjs 进主包） |
| 2 | `PdfReaderView.tsx` `toggleBookmark` | 在 `!has` 分支派发该事件（带 `relPath`）；deps 补 `relPath` |
| 3 | `PdfRailPanel.tsx` | 新增 useEffect 监听 → `setSection('bookmarks')`；带 `relPath` 过滤（同 `KB_PDF_PAGE_CHANGED` 惯例） |

**范围界定（已核实）**：**仅 PDF 有此问题**。EPUB 左栏面板**故意不出书签 Tab**（`EpubRailPanel` 头注：书签出在右栏 `ReadingSidePanel`，同一份书签不列两处），TXT 同理。故本事件只由 PDF 派发、只被 PDF 面板消费。

**验证**：`tsc --web` 0 错；新建契约 `verify-bookmark-ux.mjs` **26 项全绿**（含「移除时不切区」的 `!has` 守卫断言）；**已做变异测试**（去掉 `!has` 守卫 → 该断言立即变红，还原回绿）；PDF 全量 6 个契约脚本全绿。

---

## [x] N-2 书签备注输入框改为「默认只读、点备注才编辑」

**提出（2026-09-23，开发负责人原话）**：「左侧边栏的书签条目也需要优化，他的哪个备注输入框很容易就会被选中修改，比如我只想点击书签跳转的，结果直接点击到书签条目上的备注输入框了从而造成误改」。

**要解决什么**：原先整张卡片第二行就是一个**常驻裸 `<input>`**（`defaultValue` + `onBlur` 落库），点条目想跳页却落到输入框 ⇒ 误改备注。**根因是「可编辑区域」与「跳页区域」在视觉与命中上完全重叠**。

**口径（已拍板）**：**默认只读，点备注文本/图标才进编辑**；卡片其余处一律跳页。

**改动**（`src/components/shared/pdf/PdfBookmarkList.tsx`）：

- 新增 `editing: number | null` 状态（`null` = 全部只读）。
- **非编辑态渲染的是 `<button>`（锚点 `data-wb="pdfBookmarkEdit"`），不是只读 `<input>`** —— 只读 input 仍会吃点击、焦点行为也更绕。
- 编辑态才换成真 `<input>`，进入即自动聚焦；`Enter` 提交（走 `onBlur` 落库）、`Esc` 放弃（回滚 value 并退出）。
- 跳页按钮与删除按钮**原样保留**（改备注不吃掉既有能力）。

**验证**：同上契约的 ④ 段（含「进编辑自动聚焦 / Enter / Esc」三项）+ ⑤ 段负向断言「不再有裸 `defaultValue` input + `onBlur` 落库这一无门控形态」；**已做变异测试**（把编辑态三元改成永远 `true` → 「编辑态才渲染 input」断言立即变红，还原回绿）。

**★ 已核实：右栏本来就是这个形态，「默认只读」是既有惯例，不是新发明。** 右栏阅读侧栏（`ReadingSidePanel` 的 `ExcCard`）早就是 `noteEdit` 状态 + 点铅笔才编辑 + `{e.note && !editing}` 渲染只读文本（`:435`/`:456`/`:478`）。**⇒ PDF 左栏那个常驻裸 input 才是异类**，本项把它对齐到既有口径。故无需再担心其他处的同类误改；若将来发现别处仍有裸 input，一律按此口径收敛。

---

## [~] N-3 恢复「软件文件」入口（编辑区并入知识库后，总览态丢了这个区）

**提出（2026-09-23，开发负责人原话）**：「现在的编辑区和知识库合并了，导致了一个问题，之前的『软件文件区』没有了」。

**★ 状态：仅记录，未动手**（按开发负责人本轮要求：先攒批、再统一决定修哪些）。

## 一、这个区是做什么的（开发负责人 2026-09-23 补充说明）

> 「将一些**服务于本软件**的一些文件集中放起来，以免跟用户的知识页面之类的弄混了，导致笔记区比较臃肿。」

**⇒ 定位 = 软件自用文件与用户知识内容的分离区**，纯为「去臃肿」。这正是 `softNames` 机制的原始意图（`workspaceManager.ts:45-47`：软生成项不与用户目录混排）。

## 二、开发负责人建议放进该区的四类内容（★ 已逐条核实现状）

| # | 建议放入 | **现状核实** | 待办 |
|---|---|---|---|
| 1 | `.ignore` 文件（其中标注的文件不在笔记区显示） | **已在该区** —— `SOFT_ENTRY_NAMES = new Set(['.ignore'])`（`workspaceManager.ts:47`） | 无（恢复出口即自动带上） |
| 2 | **整个 AI 教学区文件夹** | **已在该区** —— `softNames` 动态并入 `aiTeachRootDir` 设置推导出的产物根（`workspaceManager.ts:778`） | 无（同上） |
| 3 | **给 AI 助手的约束**（每次回答都遵循） | **文件已存在** —— 全局 `{产物根}/CONSTRAINTS.md`（`aiTeachingFolders.ts:205`；AgentRunner 每轮重读注入） | ⚠️ **但它现在只作为「AI 教学文件夹的子文件」存在，不是独立入口** ⇒ 若要它**单独出现在软件文件区**，属**新增展示项**，见「待拍板 ③」 |
| 4 | **AI 教学的画像和要求**（可能含在 AI 教学文件夹中） | **文件已存在** —— `{aiTeachRootDir}/PROFILE.md`（全局画像，`aiTeachingProfile.ts:103`）；另有工作区 / 会话级 PROFILE.md（懒建） | ⚠️ 同 3：已随 AI 教学文件夹**整体**进区，但**不单独列条目** |

**★ 关键结论（这决定了工作量）**：**四类里 1、2 已经在机制内，3、4 的文件也已存在** —— 本条目**不是「新增四类内容」，而是「恢复一个已被丢掉的出口」**。恢复后 1、2 自动回来；3、4 因位于 AI 教学文件夹内部，会**随文件夹一起**出现；**只有「3 单独成为一个快捷入口」是真正的新增需求**。

## 三、根因（已定位到代码，非猜测）

「没了」的机制是**数据侧主动剔除 + 渲染侧无人承接**：

```ts
// src/components/workbench/WorkbenchLeftPanel.tsx:166-168 —— 剔出去，但没存到任何 state
const soft = new Set(res.softNames ?? [])
setDirs((res.entries ?? []).filter((e) => e.type === 'dir' && !soft.has(e.name)).map((e) => e.name))
setLoose((res.entries ?? []).filter((e) => e.type === 'file' && !soft.has(e.name)).map((e) => e.name))
```

- **数据仍在**：`ws:listDir` 照常返回 `softNames`（`workspaceManager.ts:776-781`）。
- **渲染缺失**：总览态（overview 分支）只渲染「零散文件」（`:398-408`）与仓库切换栏（`:411`），**没有任何区承接 `softNames`**。
- ⇒ 这些条目在总览态**彻底不可见、不可达**。

**同名物澄清（别修错地方）**：**文件树底部**那个「软件文件」折叠节（`VaultTree.tsx:295-316`，默认收起 + 计数）**仍在**，且**只在文件树的根层**渲染。丢的是**总览态左栏**那个 —— 即旧「编辑区」模块左栏根列表里的沉底区（`CHANGELOG.md:221` 有记）。

**性质**：`softNames` 机制本身没被废弃（`workspaceManager` 仍返回、`VaultTree` 仍消费、图谱仍按用户指定排除软件目录 `graphGroups.ts:35`）⇒ **改版落差（能力丢失），非有意移除**。

## 四、开发负责人的两条形态要求（已拍板）

1. **「跟以前的编辑区一样，只是编辑一些文档的，不做预览之类的工作」** ⇒ 该区的条目**点开是编辑**，**不要**知识库那套预览 / 沉浸阅读 / 图谱联动。
   - ★ **一处须澄清**：非文本文件（如 AI 教学产物里的图片 / PDF）在该区怎么处理？**倾向：仍按文件类型走各自默认打开方式**（图片不在该区预览，PDF 走阅读器）—— 但这是**我推测的**，须确认（见「待拍板 ①」）。
2. **「我建议放这几个东西」** ⇒ 清单见上「二」，其中 1/2 已在机制内。

## 五、待拍板（★ 已于 2026-09-24 全部拍板 —— **结果见下方「五·乙」**；本段保留作记录）

- **① 非文本文件的开法**：该区是否严格「只编辑文档」？若是，AI 教学产物里的 `.png` / `.pdf` 点了怎么办（是不显示、还是仍按默认方式打开）？
- **② 方向二选一**：
  - **(A) 总览态补一个「软件文件」折叠区** —— 与文件树底部同款形态（默认收起 + 计数）。数据侧把 `softNames` 条目**另存一份 state**（而非丢弃），渲染在「零散文件」之下。**成本低、与文件树口径一致，推荐。**
  - **(B) 不专设区，并回「零散文件」** —— 与「分离以免臃肿」的初衷冲突，**不推荐**。
- **③ 要不要给「AI 约束」单独入口**（上表 #3）：现在它只作为 AI 教学文件夹**内部**的一个文件存在。若希望它**单独在软件文件区列一条**（便于随时改「每次回答都要遵循」的规则），那是**新增快捷方式**，不是恢复 —— 需明确要 / 不要。
- **④ 该区条目要不要右键菜单**（打开 / 重命名 / 删除），还是只读可打开？（「零散文件」区现有右键菜单，见 `WorkbenchLeftPanel.tsx:416` 起）

## 五·乙、拍板结果（2026-09-24 开发负责人逐条定，★ 四个待拍板点全部已定）

| # | 问题 | **决定** |
|---|---|---|
| ① | 非文本文件（`.png` / `.pdf`）在该区怎么开？ | **按默认打开方式** —— 不做特殊处理，图片按图片、PDF 走阅读器。即「该区不做预览」这条只约束**区的入口语义**（点条目 = 打开 / 编辑），**不改各格式既有的打开行为**。 |
| ② | 方向 A 还是 B？ | **选 A** —— 总览态补一个「软件文件」折叠区（形态同文件树底部：默认收起 + 计数），数据侧把 `softNames` 条目另存一份 state 而非丢弃。 |
| ③ | 「AI 约束」要不要单独入口？ | **先不做单独入口。** ★ 但开发负责人指出**另有一个「AI 助手的约束」议题要单独讨论**（见下方「七」），**不并入本条**。 |
| ④ | 该区要不要右键菜单？ | **需要。** 口径对齐「零散文件」区现有右键菜单（打开 / 重命名 / 复制路径 / 删除到回收站），实现位置参照 `WorkbenchLeftPanel.tsx:416` 起。 |

**★ 附带澄清（记录 ① 的口径边界）**：开发负责人在本次之前定过「该区只编辑文档、不做预览之类的工作」（§四·1）。与 ① 的「按默认打开方式」合并读 ⇒ **该区不提供知识库那套预览 / 沉浸阅读 / 图谱联动**；点条目就是**打开**（文本进编辑、PDF 进阅读器、图片按默认），**不是**「在该区里内嵌渲染」。

## 六、待定稿的实现要点（方向 A + ④，供真正开修时用；**本文档不落码**）

1. **数据侧**：`WorkbenchLeftPanel.refreshRoot`（`:159-170`）现在把 `softNames` 命中项从 `dirs` / `loose` 里 `filter` 掉 —— 改为**另存一份 state**（如 `softEntries`），保留条目名 + 类型（dir/file），供新区渲染。
2. **渲染侧**：总览态在「零散文件」区（`:398-408`）之下新增「软件文件」折叠区。**形态照抄 `VaultTree.tsx:295-316`**（`mt-auto` 沉底 + `chevron` 旋转 + `.kb-collapse` + 计数），保证与文件树视图观感一致。
3. **右键菜单**（④）：新增一份 soft 区专用菜单（或把现有 `looseMenu` 泛化成「根条目菜单」，两条路径共用）。**注意**：`looseMenu` 的浮层**必须 portal 到 body** —— 左栏面板的变换容器会把 `fixed` 的包含块变成窄栏（`:416-418` 已有此坑的记录与解法）。
4. **默认收起 + 记忆**：`VaultTree` 用 localStorage 记忆开合（`:143`）—— 新区**建议同样记忆**，口径保持一致。
5. **契约**：现有 `.AGENT/scripts/workbench-shell/verify-workbench-shell.mjs` 需补「总览态软件文件区存在 + `softNames` 不被静默丢弃」的断言（**这条正是本次丢失能溜过契约全绿的原因** —— 无任何断言覆盖总览态是否承接了 `softNames`）。

## 七、★ 另开专题（不在本条范围内）

**「AI 助手的约束」议题**（2026-09-24 开发负责人明确要单独讨论）。

- **本条范围界定**：N-3 只做「恢复软件文件区」，**不含** AI 约束的任何改动。
- **已存在的相关实现（供那个专题起手，避免重复调研）**：
  - 全局约束文件：`{产物根}/CONSTRAINTS.md`（`aiTeachingFolders.ts:205`，AgentRunner **每轮重读注入**）。
  - 会话级约束：`{aiTeachRootDir}/{会话文件夹}/CONSTRAINTS.md`（`:196`）。
  - 画像三层：全局 / 工作区 / 会话 `PROFILE.md`（`aiTeachingProfile.ts:103` / `:151` / `:165`）。
  - **优先级链**（注入提示词中已明示）：用户当下消息 > 会话约束 > 全局约束 > 内置人设（`aiTeachingFolders.ts:206`）。
- **⇒ 专题起手时先读上列四处**，再决定是「暴露编辑入口」还是「改机制」。

## 八、涉及文件（方向 A + ④ 定稿后）

| 文件 | 作用 | 改动预期 |
|---|---|---|
| `src/components/workbench/WorkbenchLeftPanel.tsx` | 总览态渲染 + `refreshRoot` 的 soft 过滤 + 右键菜单 | **主改**：①保留 soft 条目（另存 state）②新增折叠区渲染 ③菜单泛化 |
| `src/components/shared/VaultTree.tsx:295-316` | 文件树底部同款折叠节 | **参照**（形态 + localStorage 记忆口径），未必改 |
| `electron/lib/workspaceManager.ts:45-47 / 776-781` | `SOFT_ENTRY_NAMES` + `softNames` 数据源 | **不用改**（名单已含 `.ignore` 与 AI 教学产物根） |
| `electron/lib/aiTeachingFolders.ts:205` | 全局 `CONSTRAINTS.md` | **本条不改** —— 归「七、另开专题」 |

> **★ 归档区段的说明**：按本文档判据「以前有没有过」——软件文件区**以前有过**，严格说该进 `F-n`（改版落差）。本条暂置 `N-n` 并按开发负责人本轮口径（「可以优化的点」）处理，标题已标注「改版落差（能力丢失）」。**若希望按 `F-n` 归档以便与其它改版落差汇总，说一声即可平移。**

---

## [~] N-4 AI 教学「编辑画像 / 要求」跳转后无返回入口

**提出（2026-09-24，开发负责人原话）**：「以前在 AI 教学区点击编辑画像和限制之类的，可以直接跳转到编辑区，然后编辑区也有对应的按钮可以直接跳转回来，现在没有了」。

**★ 状态：仅记录，未动手**（按开发负责人要求：先记录，之后一起改）。

**★ 状态判定：能力丢失（改版落差），非有意移除 —— 且两侧都留了残骸，属「拆到一半」。** 具体：**跳转的发射端完好、接收端被改道；返回端整个 Dead Code**。

## 一、原设计（有据可查）

- **去向**：`docs/ui-updates.md:648-663`「14. 画像编辑 = 跳编辑区」——三入口（选择页全局卡片 / 工作区卡片 hover / 顶栏本主题 chip）→ 新 `ensure` IPC（文件缺失则落骨架）→ **`kb-open-note { relPath, from: 'aiTeaching' }` 跳编辑区**，toast 提示可「← 返回」。
- **回向**：`App.tsx:566` 注释——「跳转来源记录——`kb-open-note` 带 `from`（如 aiTeaching），**编辑器出「← 返回 X」chip**」；`Entry6` 机制。

## 二、根因（三段链，已逐段核实）

**链路（旧）**：AI教学侧 `kb-open-note { from:'aiTeaching' }` → App 监听 → **切到 editor Tab** + 记 `from` → 编辑器页条出「← 返回 AI教学」chip → 点它回 AI 教学。

| 段 | 现状 | 证据 |
|---|---|---|
| **① 发射端（AI教学侧）** | ✅ **完好** | `ai-teaching/index.tsx:1687 / 1694 / 1703` 三处仍照常派发 `kb-open-note { relPath, from:'aiTeaching' }`（画像全局/工作区/要求）。`:1402`、`:565`、`:568`、`:3522` 亦同款。**这半没坏。** |
| **② App 接收端** | ❌ **被改道，且 `from` 被丢弃** | `App.tsx:570-580`：现在写死 `setActiveTab('knowledge')`，并把 `relPath` 转成 `kb-open-note-rel` 转交知识库文件视图。**注释（`:566`）还写着「记 `from` → 编辑器出返回 chip」，但代码里 `from` 已无人消费** ⇒ 跳转落到**知识库**（而非旧「编辑区」），**返回 chip 无处生成**。 |
| **③ 返回端（编辑器侧）** | ❌ **整个 Dead Code** | `WorkbenchPageBar.tsx:102-103` 仍声明 `lead?: React.ReactNode`（注释：「条最左端前置元素（编辑器「← 返回 X」chip）」），`:162` 也仍渲染 `{lead && …}`，但**全仓 `grep 'lead=' src/App.tsx` 零命中** ⇒ `lead` 恒为 `null`，chip 永不出现。**prop 在、接线没了。** |

**⇒ 用户体感**：点「编辑画像」→ 确实**跳了**（落到知识库文件视图，画像 md 也真打开了），但 ①**跳去的地方不是他预期的「编辑区」**（模块已退役改道）②**回不来**（chip 没了）。与 N-3 同源：编辑区退役时**只搬了「就地编辑」本身，没搬围绕它的入口与回跳**。

**一处连带**：AI 教学侧那句 toast（`:1688`）**仍在**告诉用户「编辑器顶栏可『← 返回 AI教学』」—— **这条文案现在是假的**（顶栏没有那个 chip）。属于「提示与实际不符」，修时一并改。

## 三、修复方向（待拍板，勿自行决定）

**核心问题：现在没有「编辑区」这个 Tab 了 —— 那「返回」该返回哪、chip 该长什么样？** 这是产品口径，需开发负责人定：

- **(A) 恢复「← 返回 AI教学」chip，落在知识库页条** —— 复用现成的 `WorkbenchPageBar.lead` 槽（**只要 App 把 `from` 记下来并传 `lead` 即可**，**无需新基建**），点它 `setActiveTab('aiTeaching')`。**成本最低，且是原设计的直接平移。**（`lead` 槽现成、`from` 字段现成。）
- **(B) 不设 chip，改用别的方式回跳**（如 toast 里给按钮 / 命令面板）—— 需明确。
- **(C) 跳转目标改回语义上的「编辑器」** —— 但编辑区已退役，能力进了知识库 ⇒ **大概率仍是 (A)**，只是「编辑区」这个叫法要改口。

## 四、实现要点（若选 A）

1. **App 侧**：`kb-open-note` 的 handler 里**把 `detail.from` 存起来**（新 state，如 `noteJumpFrom`），并传给 `WorkbenchPageBar` 的 `lead` 槽；**任何手动切 Tab 清除**（注释 `:567` 已写明这个口径）。
   - ⚠️ **注意现有 handler 的 `?? undefined` 判据**（`:572`）：`from` 类型是 `TabName`，AI 教学传的是 `'aiTeaching'`—— 需确认该值在 `TabName` 合法集内。
2. **渲染侧**：`lead` 里放一个 chip（「← 返回 AI教学」），点击 → `setActiveTab(from)`。**样式走既有令牌**（铁律 13），不另造过渡。
3. **文案同步**：改 `ai-teaching/index.tsx:1688` 的 toast（「编辑器顶栏」→ 新位置的说法），或直接去掉该指引。
4. **契约**：**已核实 `lead` 槽没有任何契约覆盖** —— 全仓 grep 只命中 release-notes 模块里一个**同名的无关字段**（`build-release-notes.mjs` 的 `lead`/`rest` 拆分），`WorkbenchPageBar.lead` 这条链**零断言**。**这正是本丢失能溜过契约的原因**：prop 无人喂也无人验。修时需补一条「带 `from` 的跳转会产生可用的返回 chip」断言。

## 五、★ 建议同批盘一遍「编辑区入口还丢了哪些」

本条与 N-3 都是「编辑区退役的连带」。**修的时候建议全仓盘一次**：`grep -rn "kb-open-note" src/`（十余处 dispatch 方）逐个确认——**每一个「跳编辑区」的入口，现在落到哪、还能不能回来**。已在台账内确认的两条：N-3（软件文件区）、N-4（画像/要求 + 返回 chip）。

**涉及文件**：`src/App.tsx:566-580`（主改：记 `from` + 传 `lead`）、`src/components/workbench/WorkbenchPageBar.tsx:102/162`（`lead` 槽现成，接线即可）、`src/modules/ai-teaching/index.tsx:1687/1688/1694/1703`（发射端在、文案需改）、`.AGENT/scripts/`（补契约）。

---

## [~] N-5 AI 助手独立的「要求 + 画像」（存于软件文件区）+ 面板常驻编辑入口

**提出（2026-09-24，开发负责人原话）**：「我想让 AI 助手也有相关的限制，然后还能编辑用户的画像供 AI 助手用……我想限制的存放放在软件文件区中的 AGENT 文件夹中，这里可以放用户的画像 + 会话要求。现在就来讨论一下快速编辑调换入口放在哪，需要让用户知道这个东西能够编辑的」。

**★ 状态：仅记录，未动手**（按开发负责人要求：先记录、攒批、再统一决定修哪些）。

**★ 前提澄清（这决定改动量）**：**AI 助手当前是零注入的** —— `agentService.ts:675`

```ts
const profileHint = source === 'aiTeaching' ? resolveProfilesForInjection(sessionId, getSettingReader()) : ''
// 约束三层同理，整段受 source === 'aiTeaching' 门控
```

| | AI 教学 | **AI 助手** |
|---|---|---|
| 会话 / 工作区 / 全局 约束 | ✅ 全注入 | ❌ **零** |
| 三层画像 | ✅ 注入 | ❌ **零** |

⇒ **本项不是「给助手也加上」，而是「为助手另起一套独立体系」**（见下方「与教学的关系」）。

## 一、定位（开发负责人 2026-09-24 申明）

> 「AI 助手的定位：它主要是个**工具，帮助用户处理整理文件**。」

**⇒ 与 AI 教学（学习者画像 / 教学规则）职责完全不同** —— 故**不共用**同一份要求与画像，共用是浪费。

## 二、已拍板（2026-09-24，五条）

| # | 决定 | 说明 |
|---|---|---|
| ① | **独立第二份，不与 AI 教学共用** | 助手是文件处理工具，教学是学习陪伴，两套要求互不相干 |
| ② | **存放位置 = 软件文件区里一个文件夹** | 归 N-3 的「软件自用文件」定位；该文件夹需登记进 `SOFT_ENTRY_NAMES`（`workspaceManager.ts:47`，现仅 `.ignore` + 动态 AI 教学产物根） |
| ③ | **文件夹里放两个文件：用户画像 + 会话要求** | 沿用既有文件名 `PROFILE.md` / `CONSTRAINTS.md` |
| ④ | **编辑入口 = AI 面板上一枚常驻按钮**（右栏小对话 + 中间 aiChat 标签） | 理由是开发负责人原话：「**需要让用户知道这个东西能够编辑的**」—— 常驻才具备可发现性，藏在命令面板里等于没入口 |
| ⑤ | **点入口 → 弹出层列出两个条目（要求 / 画像）→ 点哪个跳哪个**；**跳编辑器打开**（不内联编辑） | 与 AI 教学 `openProfile` 同款交互（`ai-teaching/index.tsx:1679` 起）；两个文件各有归属地，日后加第三个不用改 UI |
| ⑥ | **跳转依赖 N-4 修好** | 「跳到编辑器 + 回来」这条链当前是坏的（见 N-4）⇒ **N-4 是本项的前置**，须先修或同批修 |

**⇒ 已定 6 项（①–⑥），另有 4 项待定（见「五、待定」）。**

## 三、与既有能力的关系（★ 勿与 AI 教学那套混淆）

| 维度 | AI 教学（**勿动**） | **AI 助手（本项新建）** |
|---|---|---|
| 文件位置 | `{aiTeachRoot}/PROFILE.md`、`{工作区}/CONSTRAINTS.md` 等 | **软件文件区某文件夹** 下 `PROFILE.md` + `CONSTRAINTS.md` |
| 层数 | **三层**（全局 / 工作区 / 会话） | **一层**（本项只提「用户画像 + 会话要求」两个文件，**未提分层** ⇒ 见「五、待定」） |
| 注入门控 | `source === 'aiTeaching'` | 需新增（如 `source === 'assistant'` 分支） |
| 优先级链 | 用户消息 > 会话 > 工作区 > 全局 > 人设 | 待定（若单层则简单得多） |
| 编辑入口 | 选择页卡片 / 顶栏 chip / 命令面板（**三处**） | **面板常驻按钮**（本项已定 ④⑤） |

**⇒ `agentService.ts` 的注入段需按 `source` 分叉**：现有那段是为教学写的（含三层画像 + 三层约束 + 冲突裁决链文案），**助手这套不能直接套用**（套用会把教学措辞注入助手提示词）。

## 四、涉及文件（预估）

| 文件 | 作用 | 改动预期 |
|---|---|---|
| `electron/lib/workspaceManager.ts:47` | `SOFT_ENTRY_NAMES` | 登记新文件夹名（使其进软件文件区） |
| `electron/lib/agentService.ts:613-637 / 675` | 约束与画像注入 | **主改**：按 `source` 分叉，新增助手分支 |
| `electron/lib/`（新或复用） | 助手要求 / 画像的读写 + ensure 落骨架 | 参照 `aiTeachingProfile.ts` / `aiTeachingFolders.ts` 的口径 |
| `src/components/shared/AssistantPanel/` | AI 面板（右栏小对话 + aiChat 标签） | 新增常驻按钮 + 弹出层 |
| `src/App.tsx:566-580` | `kb-open-note` 跳转链 | **依赖 N-4** —— N-4 修好本项才可用 |

## 五、待定（★ 须开发负责人定，勿自行拍板）

- **A. 要不要分层？** 本项只提了「用户画像 + 会话要求」两个文件。但「**会话**要求」字面暗示**每会话一份** —— 那就要么按会话分文件夹（同 AI 教学），要么「会话要求」其实指「**助手通用要求**」（单文件）。**两种理解存储结构完全不同，须明确。**
- **B. 文件夹叫什么？** ★ **已发现一个命名撞车**：仓库根已有 `.AGENT/`（**开发期工程目录，且进了 git**：契约脚本 / skill / 本项目的 agent 规则）。vault 里若也叫 `AGENT`，概念上撞车；**若谁把这个仓当 vault 打开，还会真撞**。**开发负责人已定「避开、换个名字」**，但**具体叫什么尚未指定** —— 候选：`.assistant/`、`agent/`、`_agent/`（`.assistant/` 与用途最贴且字形上远离 `.AGENT`）。
- **C. 优先级链怎么写？** 若单层，则「助手要求 > 内置人设」即可；若分层，需照教学那份写清裁决链。
- **D. 「画像」对助手的具体用法** —— 助手是文件处理工具，「用户画像」对它意味着什么？（偏好？常用目录？文件命名习惯？）**这决定骨架模板写什么**。★ 开发负责人尚未展开，**建议下次一并说清**。

## 六、本条与 N-3 / N-4 的依赖关系（排期用）

```
N-3（软件文件区恢复）───► 本项 ② 的「存放位置」才可见
N-4（跳转 + 回跳链修好）─► 本项 ⑤ 的「跳编辑器打开」才可用
```

⇒ **N-5 不能单独先做** —— 它站在 N-3 与 N-4 之上。**建议顺序：N-3 → N-4 → N-5**（或三条同批）。

---

<!-- 模板（复制这段开新条）：
<!-- 示例：## [ ] N-n 一句话标题  -->

**提出（YYYY-MM-DD，开发负责人原话）**：「…」。

**要解决什么**：…

**期望交互**：…

**涉及模块 / 文件**：…

**已定 / 未定**：已定 …；未定 …（**未定的先回来问，别自行拍板**——见 `AGENTS.md`「开发流程」第 3 条）。

**与既有能力的关系**：是否与某 `F-n` / 某搁置项重叠？重叠则先说明区别。
-->
