[TOC]

# 更新日志

## v3.1.1（2026-09-13）

> **体验优化与问题修复**：一批日常使用打磨——/ 命令补全面板（已装 Skill 可显式调用）、提问卡可收起、示意图手动缩放、出题链路加固与若干交互修复。

### ✨ AI 助手

- **/ 命令补全**：输入 / 弹出候选面板（↑↓ 选择、前缀过滤），已安装 Skill 一并列出、选中即显式注入本轮对话
- **提问卡可收起**：读回答时收成一条细条，不再被占屏；选项过多时内部滚动
- **示意图手动缩放**：Ctrl +/-/0 与 Ctrl+滚轮，工具条一键回适应
- **出题更稳**：引号约束前置 + 解析层自动修复 + 失败一键重出；移除分值徽章

### 🔧 修复与优化

- 聊天输入框关闭拼写检查（红色波浪线噪音）
- 日程 AI 写工具支持修改/删除待办，周视图补删除入口
- 会话压缩（/compress）增加进行时提示，期间防重复触发

## v3.1.0（2026-09-12）

> **AI 讲明白 + 插件生态一期**：知识库接入语义检索、会话历史从「裁剪」升级为「压缩纪要」、Agent 循环可配且耗尽优雅收场；插件开放插槽 / 命令 / 事件 / 生命周期 / 声明式设置 / 渲染器六大扩展面；日程截止提醒接入系统通知；错题本退役插件双轨全内置；全站操作动效统一。

### ✨ 插件生态（一期）

- **侧栏插槽**：插件视图可挂入编辑器 / 知识库 / 博客 / 日程侧栏（此前仅知识库），无插件时界面零侵入
- **命令通道**：插件命令进入全局命令面板（`Ctrl+Shift+P`）——界面插件命令直达其视图，后台插件命令推送常驻 Worker
- **事件通道**：页面保存 / 删除、博文保存、待办完成四类宿主事件可被后台插件订阅；权限映射现有模块能力，含限流背压
- **生命周期 + 声明式设置**：后台插件获得启停通知；清单声明设置项即可在详情页自动渲染表单，值存插件私有存储
- **渲染扩展点**：插件可注册自定义 ```围栏``` 渲染器——零能力只读沙箱、高度协商、失败自动回退普通代码块
- **市场装修**：移除「错题本（插件版）」推荐条目与双态切换残留

### ✨ AI 助手

- **语义检索**：知识库检索升级为关键词 + 向量混合召回（RRF 融合），设置页可配置嵌入模型与手动重建索引
- **会话压缩**：上下文逼近预算时自动把较早对话折叠为持久化纪要（代替静默丢弃），`/compress` 指令可手动触发，纪要随仓库保存、跨会话可读
- **Agent 循环升级**：只读工具并行执行；推理轮数 / token 预算可配（默认 16 轮）；预算耗尽自动总结收场，不再报错丢进展
- **工具返回瘦身**：批量查询默认 50→20 条、单条结果硬上限 24000 字符、更早的历史结果渐进压缩——同一批 127 条错题实测 6990 → 2460 token
- **提示缓存与故障转移**：system 与工具表打缓存断点（命中量计入用量观测）；供应商级错误自动切换备用通道

### ✨ 日程

- **任务截止提醒 · 系统通知**：提醒时刻 = 截止时间 − 提前量（分钟到天可设），逾期可重复提醒；免打扰时段（默认 22:30–07:30）内静默、出时段补发一条汇总；点通知直达任务所在面板
- **周视图改造**：DDL 红线解耦排期、周任务双态、提醒条与 AI 工具扩展

### ✨ 错题本

- **退役插件双轨、全内置**：错题本工具族全部内置化，判题一律写仓库主表（存量插件数据保留迁移 / 导出 / 清理通道）
- **数据面板重做**：内部表名换成 6 张概览卡，加「按错次档位 / 按书」两条真实分布，附导出备份与分级清空

### 🎨 界面

- **目录聚焦**：编辑器 / 知识库侧栏新增准星开关——只显示当前打开文件的目录链与同级文件，其余目录（含同级零散目录）收成骨架条：悬停显原名、点击定位并退出；骨架式 / 隐藏式两档（设置 → 编辑器 → 显示），开关状态重启保留
- **知识库标签栏回归多开**：浏览只占一个预览槽，双击 / 图钉 / 右键「固定标签」后永不被浏览替换；支持中键关闭与右键「关闭其他 / 全部关闭」
- **全局动效**：面板开合 / 列表增删 / 弹层进出 / 按钮与开关反馈 / 视图切换统一动效令牌——只动 `transform` 与 `opacity`，跟随系统「减弱动态效果」自动降级
- **层级参考线**：编辑器 / 知识库 / AI 教学 左栏多层树加缩进竖线（实线 / 虚线 / 关闭可调）
- **更新说明**：升级后自动打开一次（只看中间版本号、首装不弹），亮点卡 + 完整清单双层数据，阅读记录随仓库走

### 🏗️ 内核

- **会话存储 v2**：消息按会话分文件追加写（JSONL），替代全量重写；存量数据自动迁移

## v3.0.0（2026-09-10）

> **首个正式版（结束 beta）**：本版汇总 Vault 重构以来的全部更新——数据层对齐 Obsidian（Vault 文件即数据 + 双链 + 图谱），外壳对齐 VS Code（Workbench 布局 + 编辑器组），`sql.js` 完整移除；并新增 **AI 教学** 模块、**知识图谱**、**网页剪藏**、**PDF 工具箱**、**日程表周视图** 等。

### 🏗️ 内核重构：Vault 文件化（去 sql.js）

- **数据层彻底 JSON 化**（R6 拍板 D9）：日程 / 说说 / 习惯打卡 / 体重 / 密码本 / 错题本 / 单词本等全部模块迁入 `<仓库>/.knowbase/modules/*.json`——**Vault 成为唯一真相源**，`sql.js` 与全部内联迁移完整移除，换电脑 = 拷走仓库文件夹
  - 回收站退役 `recycle_bin` 表（全 JSON 化 + 恢复路径 vault 化）；用户档案切 `user.json`；附件台账 JSON 化；习惯联动规则 JSON 化；备份导出适配纯 `.knowbase` 打包
- **仓库 = 账户**：启动仓库选择页（Obsidian 式）；标题栏仓库切换器（新增 / 删除 / 切换）；「在文件管理器中打开仓库」；**删除仓库墓碑**——回收站恢复目录后重启自动回到仓库列表；切换仓库自动失效 knowledge / graph 缓存
- **读写分工（D7）**：知识库 = 阅读器 + 结构维护（建删目录 / 空间 / 笔记本、导入、重命名、排序、拖拽）；**编辑器 = 唯一正文写入方**（Monaco + `Ctrl+S` + mtime 冲突检测）
- **知识页双态模型**：显式 `status` 字段区分草稿 / 归档；无 frontmatter id 的文件 = 草稿（知识库不显示）；编辑器保存归档页自动随写翻转
- **索引层**：`knowledgeIndex`（页面元数据）+ `GraphIndex`（双链 / 反链 / 标签）；写时失效、用时懒重建，mtime 兜底
- **附件 `attachment://vault` 协议**：页面 / 仓库移动不断链
- **`.ignore` 过滤层**：仓库根 gitignore 语法规则文件——私人目录 / 草稿在知识库、搜索、图谱、AI 检索中整体隐藏，编辑器不受影响；含规则对账提示

### 🖥️ Workbench 外壳（VS Code 化）

- **编辑器模块（新）**：Monaco 内核（worker 标准配方 + 主题由 CSS 变量动态合成）、文件树（复用 vscode-icons）、VS Code 式内联命名（去居中弹框）、根列表「软件文件」折叠节沉底
- **分栏预览（R5）**：编辑区左 Monaco 编辑 / 右实时 Markdown 渲染（取代 Live Preview 方案），预览开关 + 记忆
- **禅模式**：`Ctrl+K Z` 三档状态机（壳层隐藏 + 顶部热区 + 悬浮信息条 + 限宽居中），`Esc` 退出，顶栏入口
- **文件操作撤销栈**：`Ctrl+Z` / `Ctrl+Shift+Z` 覆盖移动 / 重命名 / 新建，编辑区与知识库**共用一份**
- **命令面板 / 快速切换器**
- **侧栏统一化**：ResizablePanel 拖拽调宽 + `Ctrl+B` 开合 + 双手柄，编辑器组侧栏槽
- **液态玻璃视觉**：`--window-radius` 大圆角 + 内容卡片壳 + 留白玻璃质感；最大化贴边（去留白 / 圆角）、最大化拖边缘 → 恢复窗口并贴随鼠标（Edge 式）；红绿灯窗控

### ✨ 新增功能

- **🎓 AI 教学模块**（原「Agent 沉浸式工作台」更名）：三栏工作台、会话 ⇄ 文件夹绑定（`{MM-DD} 标题` 命名 + 锚点扫描）、`CONSTRAINTS.md` 约束文件化、素材库结构 v3、**视觉转写**分批流水线（双模型槽位、自动筛选视觉模型、可见进度条）、自动出题、用户画像、Token 消耗可视化；**工件栏**——AI 生成的 HTML 产物经 `kbview://` 自定义协议沙箱渲染
- **🕸️ 知识图谱**（d3-force，入口在知识库左侧目录树底部）：Canvas 2D 渲染、物理动画、交互 / 过滤 / 增删动画、**目录 scope**（进入某目录只展示该目录图谱 + 跨目录关联）、节点卡片正文预览、设置持久化
- **🗓️ 日程表周视图**：排期时段网格，任务从「待安排」拖入排期、边缘拉伸调时长、未完成自动延后；拖拽改用 pointer events（HTML5 拖放静默失败根治）
- **🧰 工具箱扩充至 11 件**：
  - **PDF 工具箱**：多 PDF 合并 / 单文件页面整理（缩略图、旋转、重排、仅导出勾选页）/ 文本提取
  - **网页剪藏**：桌面端 clipperServer（仅绑 `127.0.0.1` + Origin 白名单 + Bearer token）+ MV3 薄浏览器扩展（activeTab 即点即取），defuddle 转 Markdown 落 `.knowbase/_draft/clipper`
  - **设备传输**（lanShare）：二维码 `http://ip:端口/?token` 双向互传
  - **单词本**
  - **密码本总览改版**：收藏 / 分组 / 内置强密码生成器 / 悬浮窗新增
- **🔌 插件开放（R7 / V3）**：放行 `type: code` 插件——Worker 沙箱运行时 + `PluginHostGateway` 单点能力裁决 + CSP 放行；**ed25519 签名链路**（keyring + 作者侧签名工具）；`kb.store` 插件私有存储、`files` 对话框授权能力
- **🤖 AI 工具链**：`vault.*` 读写工具族（`vaultFile` 权限域）、会话级写上限、写后广播、`web.read` 网页通读、`docs.read-text` 仓库内 PDF/PPT 文本提取、划词问 AI 宿主路由
- **📄 内置 PDF 阅读器**：编辑器打开 `.pdf` 走 `PdfReaderView`（文本层 / 大纲 / `Ctrl+F` 搜索 / 沉浸），知识库附件路由（含无扩展名 PDF 头探测）
- **日程完成反馈三档 + 四象限图标化**（含 4 个设置项）
- **设置体系重构**：S1 schema 单源化（66 key 补 UI 元数据 + 搜索索引自动生成）、S2 能力域重排（8 业务模块 → 9 能力域）、S4 NumberField 数值直输、iOS 风格胶囊开关 `SettingSwitch`

### 🎨 体验优化

- **「今日工作台」小窗四 Tab**（日程 / 打卡 / 番茄 / 导航），嵌入式与脱离态共用一个窗口
- 界面更新文档十条全量落地（最大化圆角 / 边缘拖拽 / 侧栏手柄 / 菜单 portal 定位…）
- 仓库切换器迁移至编辑器侧栏底部、活动栏编辑器提首位、工具箱显隐管理、番茄钟预设溢出修复
- 主进程控制台输出英文化（Windows 终端 GBK 乱码根治）
- 内置工具列表两层折叠 + 悬浮覆盖层；AI 面板过程实时可视化 + 本次改动清单（点击直达编辑器）

### 🐛 Bug 修复

- **AI 写工具链路**：权限不足被预过滤出模型视野导致「假成功」、写盘后未广播致保活模块不重读、`jsonStore` 静默失败使工具误报 `ok:true`——三处一并修复
- **自动保存型编辑器三铁律**（密码本条目被覆盖复盘）：待保存内容与目标 id 原子绑定、防抖 timer 置 null、全字段 update 带 `expectedUpdatedAt` 乐观锁
- **拖拽体系**：HTML5 拖放改 pointer events（静默失败根治）；pointer 手势结束补发 `click` 的误触发编辑窗拦截；跨组件拖拽携带任务本体快照
- Monaco：`MonacoEnvironment.getWorker` 注册（dev 下 worker throw 致编辑器挂不起来）、dispose 竞态与错误边界、编辑区高度塌陷
- 知识库：分类树 dict 解析（76 分类全读空）、导入后图谱为空、性能卡顿（剥离大字段 + 去 N+1）、`.ignore` 稳定性、删除走系统回收站 / 编辑器树草稿徽标
- 仓库切换：设置快照回滚 `currentVaultId`、`will-navigate` 守卫吞掉 `reload`、下拉菜单被遮挡
- 启动：无当前仓库统一走仓库选择页、blog 无仓库静默跳过拉取、首次创建仓库后默认打开编辑器、新仓库自动落欢迎文档
- 用户模块永远「加载中」；网址导航导入浏览器收藏夹 HTML；回收站保留天数非法值兜底
- dev：renderer watcher 忽略原子写临时物（chokidar EBUSY 杀死 dev 进程）

### 🔧 内部改进

- 47 个内联迁移拆分为独立文件 + 顺序索引（顺序即语义，只允许追加）
- `devbridge` UI 工具链（`/ui/screenshot` + `/ui/tree` + `ui.*` 动作）——AI 具备界面操作验收能力
- 清理锁屏功能残留（设置区 / 孤儿组件 / 设置白名单）、下线「博客读源」灰度开关
- R0–R7 路线图整体收官（数据地基 → Workbench → 编辑器与链接 → 数据层推广 → 图谱 → 分栏预览 → 去库收尾 → 插件开放）
- `tsc` 双 tsconfig 门禁 + 冒烟脚本体系（ai-teaching / clipper 安全矩阵等）

## v2.16.0（2026-09-01）

### ✨ 新增功能

- **AI Skill 独立安装**：Skill 不再只能随插件安装——「设置 → AI 工具 → Skill」页签支持**拖入 zip 包**或点击选择文件直接安装，独立安装的 Skill 可单独卸载、重装即覆盖更新
  - 两种包格式：`SKILL.md`（YAML frontmatter + 正文提示词模板，Claude/WorkBuddy 生态通用）/ `skill.json`（与插件 `contributes.skills` 同构）
  - 独立 Skill 落盘于数据目录 `skills/<id>/`，注册名 `skill.standalone.<id>`，与插件贡献（`skill.<pluginId>.<skillId>`）并存不冲突
  - 安全校验：zip 解压防路径穿越、id 合法性校验、8MB / 200 文件上限；纯声明式提示词不执行代码
- **AI 感知已配置 Skill 清单**：AgentRunner 构建 system prompt 时注入当前可用的 Skill 数量、名称与用途（描述截断防 token 膨胀），AI 明确知道自己配了多少个提示词能力包、何时该调用
- **Skill 个别停用/启用**：Skill 配置页每个 Skill 卡片带启用开关——停用后 AI 工具列表与 system prompt 清单中即时消失（文件保留，可随时重新启用）；插件贡献与独立安装的 Skill 均支持个别停用，互不影响
- **AI 联网搜索**：内置只读工具 `builtin.web.search`——零配置联网搜索（Bing 主源 + DuckDuckGo 备源，自动切换），AI 在对话中遇到时效性/本地库外信息时自动调用；结果精简为标题/链接/约 300 字摘要控制 token，计入月度用量与审计；跨模块通用能力不受模块权限限制
- **帮助文档新增「AI Skill 技能」**：说明两种安装方式、SKILL.md / skill.json 包格式示例、卸载与更新规则
- **插件市场新增 Skill 分类与标签**：带 `skills` 贡献的插件归入「Skill」分类（市场分类筛选新增 Skill 项），插件卡片与详情页显示 Skill 贡献标签；首发「Teach 教学助手」——源自社区 teach skill（MIT）的引导式教学提示词技能，让 AI 像优秀教师一样教人（评估水平→已知到未知→检查理解→实时调整→练习强化）

## v2.15.1（2026-09-01）

### 🐛 Bug 修复

- **正式版关闭后再次启动窗口闪烁闪退**：单实例锁获取失败时未短路后续启动流程（第二实例仍会初始化数据库/注册 IPC/创建窗口后退出）；托盘常驻下 `second-instance` 只 focus 不 show，隐藏窗口无法重新出现——两者叠加表现为「点 X 关闭后再启动就闪退且唤不回」。现改为锁失败立即静默退出，再次启动自动唤醒托盘中的窗口
- **番茄钟面板盖住任务栏**：番茄钟全屏面板挂载在 `<main>` 下，展开时把右侧的日程打卡任务栏一并覆盖（表现为任务栏被关闭/唤不出）。挂载点移入主内容卡片，只覆盖内容区
- **日程模块「周任务」「月任务」标签无法取消**：日程模块每次加载都兜底重建这两个默认标签，用户删除后自动回来。移除兜底（周/月总结面板本就按需创建），这两个标签转为**系统维护的来源标签**：标签管理中锁定展示（不可删、带说明），创建任务的标签选择器不再混入，任务卡片/四象限仍显示来源徽章
- **任务栏停靠动画断点**：拖拽吸附顶部时旧窗口先销毁再重建，视觉上「闪一下回到主窗口再变停靠」。重构为三种桌面形态（漂浮/顶部停靠/桌面小组件）共享同一窗口运行时切换，动画连续

### 🎨 体验优化

- **任务栏脱离进入独立漂浮中间态**：点击脱离先开出可自由拖动/调整的漂浮窗口，拖到顶部停住才吸附；吸附属于隐式切换不再被记忆，下次脱离仍是漂浮
- **任务栏高度按内容自适应**：内容变多自动跟上、变少不缩（保留手动拉大的空间）；首次开窗直接贴合内容尺寸显示，不再先默认高度再收缩；自动吸附判定增加防抖与开窗宽限
- **任务栏底部模块栏移除「设置」入口**，与主窗口模块栏职责分离
- **番茄钟状态常驻任务栏**：停靠态下任务栏模块栏上方显示番茄钟倒计时 + 状态文字（专注中/休息中/已暂停）+ 阶段进度填充条；主窗口启动的番茄钟实时同步到独立任务栏窗口，点击状态行一键回到主窗口工具箱
- **移除底部状态栏**：日期/文件类型/编码等低频信息下线；番茄钟全局激活入口下沉至 Provider（原仅状态栏监听）；清理编码设置相关的无监听死代码

## v2.15.0（2026-09-01）

### ✨ 新增功能

- **日程与打卡小窗**：主窗口之外的独立伴随窗口，把「今天要做什么 / 今天打卡了没」常驻桌面一角
  - **逾期置顶**：红色分组列出全部日期早于今天且未完成的顶层任务（按日期升序），支持一键改到今天、行内编辑时间、删除
  - **今日任务**：勾选完成、展开一级子任务、行内编辑；「任务模块」直达主窗口日程模块
  - **一句话快速添加**：自动识别日期与时间（`明天下午3点开会` / `周五 14:00 复习计网` / `下周三交作业` / `8月5号交报告` / `25号取快递`），识别结果可在预览行直接点开修改，未识别到日期默认今天
  - **今日打卡**：计划内习惯一键打卡 / 取消，显示连续天数，可快速新增习惯
  - **窗口行为**：默认贴主窗口右缘吸附并跟随移动 / 缩放；拖离即解除吸附自由摆放（位置记忆）；拖近右缘出现磁吸气泡，**松手**才吸附；跟随主窗口最小化 / 还原；宽高自由调整并记忆
  - **双向实时同步**：小窗与主窗口任一侧增删改立即反映到另一侧，跨零点自动翻到新的一天
  - 入口：标题栏右侧日历图标按钮，或全局快捷键 `Ctrl` `Alt` `S`；小窗内 `Escape` 关闭
- **任务栏与桌面互动**：托盘常驻（关闭到托盘）、任务栏可拖拽到屏幕顶部**自动停靠**（QQ 式收缩成一条贴顶悬浮），停靠态出现**模块切换栏**直接切换博客/日程/知识库等模块；桌面小组件模式鼠标穿透、位置与状态持久化，下次启动还原
- **设置搜索直达**：搜索从"只匹配左侧大项"升级为**深入每一个具体设置项**——36 个设置项建立中英文别名索引，支持拼音外英文拼写容错（子序列匹配）、多词 AND 检索、按相关度排序；下拉候选带面包屑与命中高亮、键盘上下选择、`Tab`/`→` 补全、热门推荐；右侧结果面板按大项分组列出全部命中，**点击直达该设置项**——自动切大项/切 AI 页签、滚动定位并闪烁高亮，条件渲染项（如月总结固定日期）与收起分组会自动展开
- **日程周/月任务标签**：日程模块新增「周任务」「月任务」默认标签，周总结 / 月总结创建任务时自动带上对应标签

### 🎨 体验优化

- **设置项下拉选择控件**（VS Code 设置风格）：应用主题 / 删除动画皮肤 / 侧边栏图标风格 / 字体样式改为标准下拉框——标题与说明常驻，收起只显示当前值，点开为悬浮选项列表（悬浮项底部显示说明、出厂默认值带「默认」角标、当前项带对勾），支持方向键 / Enter / Esc 键盘操作；字体选项用各字体本身渲染「Aa」预览，图标包选项保留五模块图标实时预览条
- **设置长列表默认收起**：AI 工具的「内置工具」「最近调用」改为折叠分组（标题带条数），不再一屏被长枚举占满
- **三卡对称卡片壳布局**：ActivityBar 与主内容区统一为圆角+阴影的同款卡片壳，四周留白、玻璃质感；AI 入口、说说新建按钮移入主体卡片，布局经过多轮回归收敛到三卡对称稳定版
- **知识库侧边栏条目**：三档条目大小（紧凑/标准/宽松）行高字号差距拉开，切换真正可见；「工作区」标签字号跟随设置
- **插件手绘图标重绘**：拼图块改为圆角主体+顶部凸起+右侧凹槽的标准插件造型，中心加连接点
- **界面字号细节统一**：任务栏字号对齐博客侧栏、快速添加输入框 12px→11px、博客侧栏放宽（256/320/200）

### 🐛 Bug 修复

- **修复主窗口最大化时小窗跑到屏幕外**：吸附位置此前直接取 `主窗口.x + 主窗口.width` 且从不越界检查，最大化（或右侧无空间）时整窗落在屏幕外，点开关毫无反应。现统一 clamp 回工作区；右侧空间不足时优先压缩宽度，最小宽度仍放不下才贴工作区右缘（保持独立窗口层级，用户点击主窗口即可把它调到前面）
- **修复小窗永久压在主窗口之上**：为实现「伴随最小化」曾把小窗设为主窗口子窗口（`parent`），但 Windows 强制子窗口 z-order 高于父窗口，导致小窗**永远遮挡主窗口内容、点主窗口也调不到前面**；主窗口最大化时小窗被推到右缘内侧，直接盖住任务列表与日历。现改回独立窗口，伴随最小化改由三路机制保证（平台 `minimize`/`restore` 事件 + 标题栏最小化按钮 IPC 联动 + 500ms 主窗口状态轮询兜底），任一路命中即生效且不依赖单一事件
- **修复吸附态调整宽度会意外脱离吸附**：resize 处理器此前在吸附态直接 return，宽度不落盘；而拖左缘改宽度会同时改 x，触发 move 事件被判成「用户拖离」。现吸附态尺寸独立持久化，且调整大小期间（时间窗兜底）不做拖离判定
- **修复吸附态高度被主窗口绑架**：高度此前强制等于主窗口高度，无法调整。现首次打开默认与主窗口等高，之后完全由用户掌控
- **修复磁吸在拖拽过程中抢窗口**：吸附此前挂在 `move`（拖拽中连续触发），鼠标还按着窗口就被拽走、光标与窗口错位。现改挂 `moved`（拖拽结束）触发，`move` 只负责气泡提示与拖离判定
- **修复首次打开时标题栏按钮不高亮**：可见状态广播早于 `ready-to-show`，主窗口收到的是 `visible=false`，且 `ready-to-show` 回调只 show 不再广播
- **修复小窗跨零点不刷新**：日期此前在组件挂载时算一次，而小窗是隐藏保活的常驻窗口，隔夜后日期 / 今日任务 / 打卡判定全部停在前一天。现对齐次日 00:00:05 自动翻页，并在获得焦点时兜底校正
- **修复逾期任务只覆盖两个月**：此前只查当月 + 上月，逾期三个月以上的任务永远不显示。新增 `schedule:getOverdue` 按 `date < today` 全量直查（上限 200 条）
- **修复一次操作触发两轮全量重载**：写操作同时走了 `notifyDataChanged`（事件驱动重载）和手写 `load()`，一次勾选等于 8 次 IPC 往返。现成功路径只走事件，失败时才 `load()` 回滚乐观更新
- **修复打卡没有乐观更新**：任务勾选有本地先行、打卡却没有，两者手感不一致。现已统一
- **修复设置模块在某些状态下进不去**：点击设置无响应的死循环路径已排除
- **修复测试版无法启动**：safe-delete 误拦 vite 清空 out 目录 + dev 模式重复打包导致 IPC 重复注册
- **修复任务栏/主体布局多轮回归问题**：反复拖宽窗口右侧大块白空、三卡间分割线丢失、任务栏收起白条、日期「今天·8月31日 周一」中间被截断等，已在三卡对称稳定版中一并解决

### 🔧 内部改进

- **跨窗口数据总线独立**：`data:notify` → `kb:data-changed` 从 `dayPanelWindow.ts` 移到新的 `electron/main/windowBus.ts`。它是全应用级能力（schedule ↔ habit ↔ 小窗 ↔ 未来新窗口），此前寄生在小窗模块里，小窗一拆同步就断
- **新增文档**：帮助中心「日程与打卡小窗」；`键盘快捷键` 补充小窗条目；README 模块表与快捷键列表同步
- **移除锁屏功能**（随半透明玻璃感改版一并清理）

## v2.14.0（2026-08-31）

### 🎨 体验优化

- **全局更新状态中枢**（新增 `src/lib/updateStore.ts`）：标题栏与设置 → 高级共用同一份数据源（`useSyncExternalStore` 单例）——标题栏开始下载 → 设置页实时显示同一进度；任一页暂停/取消，另一页立即同步。此前两处各自维护 state 互不感知，下载进度无法跨视图同步
- **标题栏更新入口升级**：有新版/下载中/失败 → 点击展开下拉面板（更新说明摘要 Markdown 渲染、立即下载、进度条 + 暂停/取消、重试、去设置页管理）；已下载 → 点击直接运行安装。此前入口点击行为单一、下载失败进度无声消失无任何提示
- **下载失败可重试**：失败态按原因结构化分流——坏字节类（大小/SHA512 校验不符）提示「服务器文件可能未传完整」并提供「重新下载」+「更换镜像重试」（自动聚焦镜像输入框，断点续传）；网络类提供「重试」+ 镜像引导。设置页与标题栏均生效
- **latest.yml 缺失预警**：下载仍按大小校验放行，但两处 UI 明确提示「更新源文件不完整（latest.yml 缺失）」及发布页手动下载指引，不再等到安装时才报无意义的 integrity check failed
- **更新日志展示**：标题栏下拉面板同样支持折叠查看 Release notes（与设置页一致，Markdown 渲染）

### 🔧 内部改进

- **失败原因结构化**（主进程 `updateService.ts`）：`UpdateResult` 新增 `reason`（size-mismatch / sha512-mismatch / network / channel-all-failed / cancelled / unknown）与 `step`（download / verify / sha512）诊断字段，UI 文案可精准区分「镜像坏字节」「服务器文件不完整」「纯网络波动」
- **新增发布脚本**：`scripts/publish-release.py`——安装包发布三件套完整性门：①exe + `.blockmap` + `latest.yml` 必须同时存在；②latest.yml 的 sha512/size 与本地 exe 实测一致；③版本号一致；通过后 `gh release` 一次上传三件套并回读远端资产列表验证齐全。杜绝 v2.13.0「exe 先传、元数据后补」窗口期事故复发
- **安装包自动清理**：安装程序成功拉起后记录标记，下次以新版本启动即删除旧安装包；安装被取消/失败（版本未变）则保留供重试
- **新增文档**：`docs/auto-update-improvements.md`——自动更新链路问题清单、优化方案与实现记录（含 v2.13.0 发布事故复盘）

## v2.13.0（2026-08-30）

### ✨ 新增功能

- **PDF 工具箱**（工具箱新增内置工具）：多 PDF 合并（按列表顺序）；单文件页面整理——逐页缩略图预览、勾选「仅导出勾选页」、单页删除/旋转（横竖版自适应）/前后移动，重排后导出新 PDF；**提取文本**——按整理后的页序抽取文字（按坐标还原阅读顺序）存为 txt，配合划词翻译与生词本直接进入英语阅读闭环；扫描件会明确提示需 OCR。结构化处理走主进程 pdf-lib（纯 JS 零原生依赖），缩略图与文本提取走渲染层 pdf.js（worker 经 vite 打包为同源资源，与内置 PDF 阅读器同方案）；导出统一走系统存盘对话框，取消不落盘
- **阅读脚注**：Markdown 新增行内脚注语法 `word^[标注]`——带脚注的词显示虚线下划线 + 上标「注」，点击弹出自己的标注气泡（点外部关闭）。编辑器工具栏新增「脚注」按钮（便利贴图标）：选中单词后点击自动包裹并选中占位文字，直接输入即完成标注。脚注存在 markdown 源内，知识库预览/沉浸阅读/博客等所有 MarkdownPreview 场景通用，与划词翻译（查词）互补——查完把关键释义写成脚注，下次读到点开即回忆
- **字体扩充 4 → 12 套**（设置 → 编辑器）：新增 等线 / 黑体 / 思源宋体 / 宋体 / 仿宋 / 楷体 / 霞鹜文楷 / 西文衬线（Times+Georgia）；每套带兜底链，未安装的字体自动回落到相近效果（如霞鹜文楷→楷体→宋体）。英语阅读场景推荐思源宋体/西文衬线
- **C 级模块插件系统**（插件体系新等级）：插件可声明**自有数据表**（`plugin_<id>_*` 命名空间，结构化 CRUD、禁止任意 SQL、列名白名单 + 参数绑定 + 行数上限），申请 `data` / `knowledge` / `navigation` 能力，并注册**视图挂载点**（如知识空间侧边栏入口 + 全屏容器）。安全：等级默认不开放需显式勾选、能力逐项授权、未授权调用 deny + 审计、卸载前数据导出兜底
- **错题本插件版**（knowbase.quizbook）：错题本彻底插件化——数据存插件自有表（书架按科目分、书内按章节归档、标签/掌握机制同内置），**判题上报在主进程按形态路由**（插件版自动写插件表），重刷复用宿主刷题器（判题仍写插件表）；内置版保留为回退（设置可切）。已上架插件市场（registry v19，含图标）；插件页「已安装」列表提供一键安装 + 官方推荐行
- **错题本归档细化**（内置版同步受益）：章节路径继承（快照补记笔记本以下 folder 层级并回填存量）、多维标签体系（考点/题型/难度/关键词四类 + 批量打标 + 标签云筛选）、掌握机制（连续答对 2 次移出）、统计面板（待复习/已掌握/今日错/正确率）、卡片备注与跳回源页面

### 🎨 体验优化

- **划词翻译卡片智能定位**：卡片改为「先渲染量高度、再摆放」——优先出现在选区正下方，空间不足自动翻到上方，仍放不下时横向移到选区侧旁，任何位置划词都**不再遮住选中的原文**、不再溢出屏幕；定位完成前透明过渡，不闪跳
- **知识库侧栏窄栏优化**（章节面板 + 笔记本列表同款修复）：页面行的文件类型徽章（MD 等）与收藏星改为悬浮显示（图标本身已表达类型），排序按钮的 `opacity-0` 隐藏改为真隐藏（原先虽不可见但持续占 ~40px 布局）——窄侧栏下页面名不再被右侧按钮群挤没

### 🔧 内部改进

- **新增依赖**：`pdf-lib`（主进程 PDF 结构化操作）、`pdfjs-dist`（渲染层渲染/文本提取，复用既有内置 PDF 阅读器同版本）
- **新增文档**：`docs/pdf-annotation-roadmap.md`——PDF 注释层编辑（高亮/批注/涂盖/烧进导出）后续实现方案，明确流式重排编辑不做及理由
- **新增脚本**：`scripts/seed-wordbook-demo.py`（单词本示例数据注入，需在应用关闭时运行）
- **新增脚本**：`scripts/publish-quizbook.py`——错题本插件一键上架（质量门 + registry/CHANGELOG 生成 + git data API 链原子提交 + sha256 验证，支持版本更新与图标上传）
- **新增迁移**：`052_quiz_mastery`（quiz_records 加 streak_correct/note）、`053_quiz_tags`（加 source_chapter + quiz_tags/quiz_record_tags 表 + 存量章节回填）
- **新增模块**：`electron/lib/pluginDataStore.ts`（插件自有表存储）、`electron/lib/quizMigration.ts`（错题本主表 ⇄ 插件表迁移/备份/回滚）、`src/components/shared/PluginFrame.tsx`（沙箱 iframe 宿主 + kb-plugin 桥：data.* / clipboard / theme / host.review / toast）
- **错题本数据迁移面板**：错题本顶栏「迁移」入口——主表/插件表行数对比、dry-run 预览、迁移（自动备份）/回滚/导出/清空，P2 阶段数据搬迁的安全通道

## v2.12.1（2026-08-29）

### 🐛 Bug 修复

- **修复 408 真题题图破图**：知识库真题页面存在 124 个"悬空附件引用"（页面引用附件但附件表无记录，重导入/清理时附件行丢失）——已从插件包逐题重建附件记录与文件（兼容三种题图命名），dev 与正式版库悬空引用全部清零
- **修复插件下载/应用更新 404**：默认下载镜像 `gh.dpik.top` 已失效（返回 500），下载链路兜底 jsDelivr 对 >20MB 包恒 404——默认镜像改为可用的 `gh-proxy.com`（设置里的镜像可随时替换，留空=直连）
- **修复 408 大题解析被嵌套代码块截断**（408 插件 v1.2.1 已发布市场）：spoiler-answer 围栏内嵌套 ```c 是 markdown 非法写法，会提前截断答案折叠——解析内代码块全部改为缩进写法，2014 41/42 等 126 道大题重新修复，全量审计 846 个折叠块零异常
- **修复刷题模式题目贴顶**：最大化窗口下题目垂直居中展示

### 🔧 内部改进

- **fix-408-pack.py 重写**：spoiler 提取改状态机（正确识别嵌套代码块），嵌套围栏自动转换为缩进代码块（围栏内安全写法），1.1.0 源可一键修复打包
- **知识包排版规范补充**：明确禁止在 spoiler-answer 内嵌套围栏代码块（改为缩进写法），杜绝排版错乱复发
- **删除动画内置默认回归纯红进度条**（与删除进度同步），插件 `deleteFx` 皮肤自定义机制保留（设置 → 外观可切换）
- **新增 `publish-knowledge-pack.py`**：知识包一键发布工具（质量门校验 → 生成 registry/CHANGELOG → git data API 链式提交 → 下载验证）

## v2.12.0（2026-08-29）

### ✨ 新增功能

- **知识包刷题判题模式**：真题页面选择题自动渲染为可点选判题卡片——答对自动滚动到下一题、答错高亮正确答案并展开解析（公式/题图全支持）；「刷题模式」逐题全屏沉浸刷题——答对自动下一题、答错看解析后继续、结束出得分与错题回顾，键盘 1-4/A-D/Enter 全程操作。双轨解析兼容 408 旧格式与新 ```quiz 围栏规范，数学/英语/政治等未来知识包开箱即用
- **错题本与题目收藏**：刷题自动积累错题、可星标收藏题目；「错题本」视图跨页面聚合（全部/收藏/错题），支持自定义分组、取消收藏、移出（重刷全对自动移出）；题目快照存储——原页面更新后错题仍可完整回看
- **删除动画与删除体验优化**：删除大空间/大目录不再卡顿——删除动画与真实删除进度同步（删除中红色吞噬进度条持续、完成瞬间吞满消失），删除失败自动回退条目恢复；动画外观开放插件自定义（新增 `deleteFx` 贡献类型，S 级纯内容：装饰 SVG/粒子/颜色/时长），设置 → 外观可切换皮肤
- **知识包大题解析排版修复**：历年真题全部 126 道大题解析重排为「加粗小标题 + 独立成段 + 代码块归属正确」；渲染层自动兜底断行（句末标点 + 小问编号）；408 插件 v1.2.0 已发布市场，其他用户更新后重导入即得正确排版
- **标签体验优化**：知识包自动导入的机器溯源标签（kb-* 前缀，应用零引用冗余）默认隐藏，页面标签只留可读的用途标签；标签条新增用途说明 tooltip（Ctrl+K 按标签筛选）
- **考研政治知识包 v1.0.0**（待发布市场）：六大模块知识点（马原/毛中特/新思想/史纲/思修法基/形策当代）+ 时政专题 + 刷题库 + 分析题模板 + 历年真题考点回顾 + 背诵手册，内容依据考研政治大纲与统编教材整理
- **刷题模式垂直居中**：最大化窗口下题目垂直居中展示

### 🐛 Bug 修复

- **修复选择题解析错乱**：408 真题选择题最后一个选项被错误拼接 spoiler 答案解析内容——解析器改为跳过答案折叠块，197 道全量校验答案与原文 100% 一致
- **修复删除动画与删除进度脱节**：删除动画不再固定时长播完——动画持续到删除真正完成（删除中持续推进 + 龙头/进度条循环），避免"动画放完条目却还没删掉"的错觉
- **修复刷题模式题目贴顶**：最大化窗口下题目垂直居中

### 🔧 内部改进

- **数据库迁移 049**：新增 `quiz_records` 表（收藏+错题合一，UNIQUE(page_id, quiz_no) 去重计数，source_space 快照区分知识包来源）
- **deleteFx 插件贡献类型**：删除动画皮肤开放插件化（S 级内容校验：SVG ≤16KB、禁 script/事件属性）
- **新增脚本**：`fix-quiz-answer-layout.py`（数据库大题解析批量修复）、`fix-408-pack.py`（知识包源修复 + 版本打包）、`pack-knowledge-pack.cjs`（知识包打包工具）
- **新增文档**：刷题判题模式方案、错题本与收藏方案、知识包大题解析排版规范、deleteFx 皮肤插件规范、考研数学/政治内容来源收集

## v2.11.0（2026-08-29）

### ✨ 新增功能

- **习惯打卡跨模块联动**：习惯可绑定其他模块的行为目标并自动完成——写博客日志（按字数阈值）、完成番茄专注（按当天场次）、完成日程任务（按当天完成数）、新建知识页面（按当天页面数）达标即自动打卡并推送远程监督。口径为「源表反查」：业务模块只上报行为，现值由统一服务按业务日期从源表反查，服务无状态——重启不丢进度、漏触发自动补上、导入备份后自愈；`UNIQUE(habit_id, date)` + 新行判定保证重复自动保存不重复打卡、不重复推送。补写昨天日志打昨天的卡，达标后不自动撤销（可手动取消），自动打卡在日历中以浅色 + ⚡ 与手动区分。数据导出/导入纳入联动规则
- **侧边栏图标风格设置**：设置 → 外观新增「侧边栏图标」选择列表，内置 手绘（默认）与 经典细线（1.4.0 回归）两包，实时预览、即选即生效；插件可通过 `sidebarIcons` 贡献追加图标包到列表（内容级 S 级安全审查）
- **开发者工具模块**（仅 DEV 可见，打包版不可见）：可扩展工具壳 + 首个工具「帮助文档编辑器」——可视化新建/编辑/重命名/删除帮助文档，中文文件名直接写回 `src/modules/help/docs` 入 git，Vite 热加载即时生效；左右双侧栏均可收放伸缩
- **模型级可用性测试**：AI 设置中选中具体模型后可一键实测其真实可用（发送 `max_tokens=1` 最小补全），可查出「供应商通但模型未开通 / 无权限 / ID 拼错」，结果内联显示耗时与错误详情，三适配器（OpenAI 兼容 / Ollama / Anthropic）均支持
- **博客卡片标注周/月总结日**：博客卡片在周总结日显示「周总结」、月总结日显示「月总结」徽章（月总结优先），与详情页总结面板口径一致；总结面板统计改为实时跟随设置变动，改动周/月总结日期后数据即刷，不再停留在旧窗口
- **自动更新下载可控**：下载中可暂停 / 取消，暂停后可继续（断点续传）；中途更换镜像源时自动检测并按新镜像纠错续传，不再因换源导致下载错乱
- **AI 测试桥**（仅 DEV）：让 AI 无需人工点击即可自动化验证应用（CDP 驱动），配合 `scripts/ui-walk-verify.cjs` 等验收脚本形成可重复的冒烟回归

### 🐛 Bug 修复

- **修复删除打卡习惯后全应用无法输入**：根因是 Electron 原生 `window.confirm` 关闭后破坏渲染进程键盘焦点——渲染层全部 7 处原生对话框（习惯删除、书签/分类删除、wiki 空链接建页确认等）替换为应用内 ConfirmDialog，`window.confirm/alert/prompt` 归零
- **修复同日双篇日记**：快速切换博文时残留的防抖自动保存会把旧日期写入新条目——编辑器按条目重挂载 + 保存日期时数据库层防重（目标日期已有其他条目则拒绝并提示），保存失败以 toast 提示不再静默
- **修复博文字数统计恒为 0**：`word_count` 此前仅在创建时写入 0、更新从不维护——现创建/更新同步按「去空白字符数」维护，供统计与自动打卡使用

### 🔧 内部改进

- **数据库迁移拆分**：47 个内联迁移拆分为独立文件并建立顺序索引（顺序即语义，只允许追加），新增迁移更安全、可读、可审

## v2.10.1（2026-08-28）

### 🐛 Bug 修复

- **修复自动更新"下载成功但安装报错"**：根因是镜像节点（gh.dpik.top）缓存曾返回坏字节，应用下载后无任何校验，坏字节静默通过直到 NSIS 安装阶段才报 `Installer integrity check has failed`。本次三重防护：①大小前置校验（content-length 与期望不符即弃，避免白下载整包）；②sha512 完整性校验（下载后与 release 的 latest.yml 比对）；③慢速换道（15 秒内不足 2MB 自动切下一节点）；候选通道扩展为 用户镜像 → gh.dpik.top / gh-proxy.com / cdn.gh-proxy.com → 直连 逐级重试，坏字节自动换道；另修复 preload 丢弃 size 参数（导致校验失效）。实测：gh.dpik.top 坏字节被拒 → 自动换 gh-proxy.com → sha512 通过 → 产物哈希与本地打包完全一致

## v2.10.0（2026-08-28）

### 🎨 UI 优化

- **导出模块并入工具箱**：数据导出从独立模块（侧边栏页签）迁入工具箱成为第 8 个工具——完整保留勾选模块导出 / 备份历史 / 进度面板能力，新增统一的返回头部；删除原导出模块（`src/modules/export`），活动栏与 TabName 类型同步移除；README / 新手引导文案相应更新
- **修复「设置与主题」点击黑屏崩溃**：活动栏溢出菜单中残留的 `HelpIcon` 引用随上一轮导入清理一并被删，点击展开菜单即 ReferenceError 崩树——彻底移除该菜单中的「帮助」项（用户菜单与 help:open 仍为帮助入口），菜单展开/进入设置全链路回归通过
- **移除侧边栏冗余的「帮助」模块**：帮助入口已收进左下角用户菜单，活动栏不再单列（`help:open` 事件与帮助页面本身保留，用户菜单 / 深链均可直达）
- **修复状态栏左下角乱码**：日期项源码中的 GBK 乱码（原 📅 emoji 被误读为「馃搮」）替换为 Calendar 图标 + 日期，各主题下显示正常
- **设置侧边栏版本号跟随当前版本**：原硬编码 `v1.3.0`（多年未更新）改为挂载时读取 `getAppVersion()` 动态显示

### 🐛 Bug 修复

- **知识包插件禁用开关真正生效**：此前知识类插件的导入状态/执行不读取 `enabled`，禁用形同虚设——现禁用后导入状态锁定 `disabled`、导入与更新被拒并提示；UI 显示「已禁用」置灰按钮，启停联动即时刷新；与其他类型插件行为一致

### ✨ 新增功能

- **README 与新手引导补全**：README 功能模块表新增 **AI 助手**（模型网关 / CC Switch 一键导入 / 工具调用审计 / 上下文感知"边看边问" / MCP / Skill / 按模块权限）与 **插件**（官方市场 / S·A·B 三级安全分级 / 主题包 / 番茄预设 / C++ 文档速查 / 408 考研知识包 / 冲突管理面板）两行，其他特性补标题栏更新徽章与镜像加速，安全设计补 AI 与插件安全条目
- **新手引导升级为 6 步**：核心功能一览扩至 8 张卡片（新增 AI 助手 / 插件），新增「AI 助手与插件」专属步骤（含 CC Switch 导入与按模块授权提示），快捷键列表补 `Ctrl+J`
- **强密码生成器由内置转市场插件**（应用侧）：不再随应用分发，改为市场安装（插件包与上架详情见插件仓库更新日志）；内置插件落位逻辑新增存量清理——不再随应用分发的内置插件自动降级为普通插件（解锁卸载）

## v2.9.2（2026-08-27）

### 🐛 Bug 修复

- **修复 AI 助手"去配置模型"跳错页**：原先跳到设置的外观节——改为直达 设置 → AI 工具 → 模型 页签；跳转通道补齐三处：模块评估期捕获监听（未挂载时 detail 不丢）、组件内监听消费（保活期间重复跳转可达）、AiToolsView 支持 initialTab 深链

### 🎨 主题适配

- **日程任务模块主题色修复**：清除"新建任务"弹窗与任务列表的旧 VS Code 硬编码调色板残留（`#007acc20/#e8112320/#569cd6/#d16969/#4a4a4a/#5a5a5a/#4fc1ff`）——任务类型三按钮（⚡琐碎/📋计划类/⏰截止类）选中底色、四象限坐标轴与分组标题、子任务复选框、删除悬停底色等全部改走主题变量（accent/danger/border-color 透明度组合），第三方主题包下不再突兀
- **主题合集 v1.4.1**：GitHub Dark / GitHub Light / 护眼米白纸感 / 赛博朋克夜城 4 套主题补齐缺失的 `--warning-bg` 变量（此前"琐碎"等警示选中底色在这些主题下退化为透明）

### ✨ 新增功能

- **插件下载走代理 + 后台化**：市场插件安装下载复用「下载镜像」加速（ghproxy 前缀节点优先，jsDelivr 兜底，镜像→CDN→直连自动回退，失败逐候选诊断信息）；下载改为流式——连接 30s / 空闲 60s 看门狗取代原"整请求 15 秒"误杀（大包必炸）；安装不再阻塞界面，随时可离开插件页，**StatusBar 全局指示条**实时显示「插件名 + 百分比/MB + 所用节点」，完成 toast 提示；registry 拉取同步走镜像优先（jsDelivr 24h 缓存曾致市场列表陈旧 404）
- **标题栏更新徽章一键更新**：启动 6 秒后静默检查更新（失败按 10s 自动重试至多 3 次），发现新版本时锁屏键旁出现带呼吸点的 ⬇ 徽章；点击即开始下载（图标转为实时进度），完成后变绿色 ▶，再点一键运行安装程序——全程无需进入设置页
- **冲突管理面板（408 计划 M3 完成）**：内容包导入完成后，因「本地已修改」等被保护性跳过的页面以独立面板可视化——清单含页标题与原因徽章、支持全选/勾选后**按页强制覆盖**（导入链路新增 `forceExternalIds` 选择性覆盖参数），「保留我的修改」一键放弃；无冲突时不打扰
- **更新下载镜像加速**：设置 → 高级新增「下载镜像」输入框（ghproxy 前缀协议，默认 `https://gh.dpik.top`），失效可随时替换或留空直连；下载按 镜像→直连 顺序自动回退，HTML 壳页守卫防坏代理；主进程对未配置过的用户同样应用默认镜像（实测 118MB 安装包：直连约 35 分钟 → 镜像约 15 秒）

## v2.9.0（2026-08-26）

### ✨ 新增功能

- **AI Agent 工具地基**（设置 → AI 工具，五大页签）：
  - **统一工具注册表 ToolRegistry**：内置只读工具 / MCP 动态工具 / Skill 提示词资产三类来源统一登记、发现与调用；入参 JSON Schema 校验、全程审计、月度调用上限
  - **内置只读工具 ×6**：知识库搜索/读全文、习惯列表/统计（连续天数·完成率）、书签搜索、番茄专注统计——输出面向 LLM 的紧凑结构
  - **MCP 客户端管理器**：stdio / SSE / Streamable HTTP 三传输；三步添加向导（stdio 黄条警告 + 双重确认）；连通性测试与工具预览；启停即上下线；并发上限 5、单调用超时 30s、响应 256KB 截断；环境变量 DPAPI 加密落盘
  - **Skill 技能包**：插件贡献类型扩展 `contributes.skills`（声明式提示词 + 变量占位符）；官方示例包「周报撰写 / 知识摘要」随应用分发；变量替换调用、一键复制提示词
  - **模型网关 Model Gateway**：OpenAI 兼容 / Ollama 本地 / Anthropic 三类供应商；Key DPAPI 加密存储（渲染层永不可见）；连通性测试、模型发现、默认模型；月度 token 预算硬限制（BUDGET_EXCEEDED 拦截）
  - **CC Switch 一键导入**：自动读取本机 `~/.cc-switch/cc-switch.db`，勾选即导入已配置的供应商（名称/地址/Key 全自动带入，Key 即时 DPAPI 加密）；未安装时提供获取指引与重扫
  - **崩溃黑匣子**：主进程异常/渲染进程崩溃自动落盘 `userData/crash-log.txt`（含堆栈与退出码）
  - **AI 对话（最小 AgentRunner）**：自然语言驱动本地工具——LLM 决策 → 注册表执行 → 结果回喂（≤8 轮），展示完整调用轨迹；工具执行与手动调用同一套审计/限额链路
  - **全局 AI 助手侧栏（方案 B）**：任意界面 `Ctrl+J` 或右下角悬浮按钮唤起，宽度可拖拽并记忆；**会话留存**——历史会话抽屉（自动标题/切换/删除级联清消息），重启完整还原消息与调用轨迹；**上下文感知一期**——在知识库打开某页面时提问自动附带该页面内容（输入框上方徽章可见），实现"边看边问"；设置中不再保留对话页签
  - **手动填模型 ID**：供应商卡片支持直接输入模型名（应对上游 /models 未列出但实际可用的模型）
  - **AI 模块权限**：设置 → AI 工具 → 权限页签，按模块控制能力边界（禁止/只读/读写）；被禁工具对 AI 完全不可见 + invoke 层硬校验双防线；拒绝操作留审计（`tool.invoke.denied`）
  - **写入类工具 ×5**（默认关闭，按模块放行）：知识库新建页面/追加内容、博客写日记（每天一篇约束沿用）、日程创建待办（四象限）、习惯按名称打卡；另补日程查询读工具
  - **对话体验**：停止生成（中断 LLM 请求与工具循环）；主内容区选中文本浮出「问 AI」按钮（一次性上下文随提问附带）；消息时间戳 + 悬停复制；面板拖窄至 320px 以下松手整体关闭（snap）；LLM/网络错误友好化提示（限频/欠费/断网分类文案）
  - **消息操作**：AI 回复按 Markdown 渲染（代码高亮横向滚动、表格滚动、链接可点击）；悬停操作——复制、删除单条回复、重新生成最后回复、编辑用户消息并重推其后回复（均落库生效）

### 🔒 安全设计

- stdio 型 MCP 会执行本机命令：未勾选「我了解将执行此命令」无法保存/测试，且新条目默认禁用
- 非本机 LLM 地址强制 https；API Key 明文只在主进程内存瞬时存在，崩溃转储/日志/审计均不可见（e2e 断言无明文泄露）
- AI 工具调用仅限内部入口，未向插件开放 capability

### 🐛 Bug 修复

- 修复以脚本路径直接启动时 `getAppPath()` 偏移导致内置插件永不落位的问题（`npm run start` 模式）
- 修复 Markdown 编辑器主题硬编码（vs/vs-dark）：Monaco 现由应用 CSS 变量动态合成主题，插件主题下编辑区即时跟随（新增 editorTheme 桥）
- 修复 AI 助手会话抽屉遮挡输入区：抽屉改为仅覆盖消息列表，上下文徽章与输入框始终可见可输入
- 修复会话抽屉关闭后透明遮罩残留：Tailwind v4 过渡属性为 translate（非 transform），transitionend 永不匹配导致抽屉/遮罩 DOM 永不卸载，隐形遮罩挡住消息区使滚轮与点击全部失效——改为定时器兜底卸载（240ms）
- 修复 AI 回复期间切换会话导致回复串视图：回复返回时校验当前会话，已切换则提示"回复已保存到原会话"
- AI 回复进行中再次发送不再被静默忽略：给出"正在回复中"提示（可点击停止）
- 未授权模块的 AI 拒绝改为可操作指引（system prompt 动态附加未授权模块清单与权限页开启路径）；权限页标注"修改即时生效，无需重启"

## v2.9.1（2026-08-26）

### 🐛 Bug 修复

- **修复 AI 助手发送消息后黑屏崩溃**：消息列表渲染的编辑态判定增加 id 空值防护——发送后列表刷新渲染时，实时消息（尚未落库、无 id）与编辑态 id 比较路径存在未捕获异常风险，会导致整棵组件树卸载（黑屏）；现已防护并实测「发送 → 回复渲染」全流程正常
- **修复模型供应商可用状态过期**：面板每次打开时重新检查供应商可用性（此前仅挂载时检查一次，先开面板、后配置好模型回来仍显示「未配置」引导态）

## v2.8.0

### ✨ 新增功能

- **插件安全分级体系(S/A/B 三级制)**:
  - **S 级 · 内容级**(绿色「内容」):纯静态资源(主题 / 博客模板 / 帮助文档 / 番茄钟预设),安装免授权
  - **A 级 · 数据级**(黄色「数据写入」):会写入数据库的贡献(习惯预设 / 网址包 / 自动化规则预留),安装与更新引入新写入能力时弹窗列明目标模块、需知情确认
  - **B 级 · 能力级**(红色「增强能力」):UI 沙箱插件,按能力清单(剪贴板写入 / 主题变量注入)逐项勾选授权,可随时撤销
- **防骗标机制**:等级由主进程按贡献内容强算,与插件自报值取高;`code` 类型维持拒收
- ~~策略开关~~(v2.8.1 移除):安全等级不再提供用户侧筛选与安装策略选择,仅作为信息在插件详情页展示;后端默认放行全部等级,分级价值保留在授权弹窗与审计
- **能力授权持久化与运行时执行**:UI 插件桥接消息按授权白名单放行,未授权调用回复 denied 并写入审计;撤销授权即时生效
- **行为审计日志**:安装 / 更新 / 授权变更 / 数据导入 / 可疑调用拒绝全记录(新表 plugin_audit_log),插件详情页「最近活动」可查、可一键清空
- **等级筛选与标识**:~~插件列表等级筛选 chips、列表与详情页等级徽章~~(v2.8.1 收敛:仅详情页保留等级信息,列表不再展示)、工具箱卡片等级色点
- 存量兼容:旧插件启动时自动判级回填;旧 UI 插件按等价现状继承授权,更新时重新确认

### 🔧 v2.8.1 调整

- **市场精简**:下架 6 个内容较薄的示例型插件(Git 入门教程包 / 考研 408 预备包 / 算法刷题站合辑 / 博客模板精选 / Git 与 Markdown 速查 / 学习资源示例包),仓库同步清理对应安装包与图标(含被主题合集取代的 4 个单主题旧包);随后下架 AI 工具导航,市场保留 主题合集 / 番茄预设Pro / C++文档速查
- **分类收敛为三类**:外观(改变软件样式)/ 工具(提供操作能力)/ 知识包(纯导入资源);分类筛选 chips 固化为这三类
- **knowledgePages schema v2**:支持 `space`(显式空间名)+ `notebooks[]` 多笔记本形态(v1 单笔记本完全兼容);空间优先原则强化——导入第一步永远是新建学习空间,同名自动加后缀,绝不写入用户已有层级
- **授权弹窗增强**:内容型插件安装前明示「将在知识库中新建学习空间《X》(含 N 笔记本 · M 页)」
- **KaTeX 数学公式渲染**:知识库/博客等所有 Markdown 预览支持 $行内$ 与 $$块级$$ 公式(remark-math + rehype-katex)
- **主题合集 v1.3.0 · 手绘线条 v2 重绘**:落地 sketch 变量基建(--radius-card/--radius-btn/--shadow-card/--divider-style 等,默认值与既有视觉一致零回归);新效果=纸面提亮 + 石墨线框 + 蜡笔红强调 + 8 值抖动圆角 + 偏移铅笔阴影 + 虚线分隔/标题线 + 波浪链接下划线 + 双线引用条 + 记号笔黄警示底;合集源收编于 `resources/market-plugins/themes-collection/`
- **知识库章节/页面列表可折叠**:章节过多时点击「章节」标题收起列表(展开态限高 38vh 独立滚动,不再挤压页面区);「页面」标题同样可收起让位;标题行带数量角标
- **主题合集 v1.4.0**:新增「**OpenCode 终端灰**」主题——暖灰单色系(opencode.ai 品牌色板)、墨黑强调色、深墨活动栏锚点,克制平直终端感;与手绘线条气质互为对照
- **408 知识包 v1.1.0 · 历年真题导入**:2009-2026 共 18 套 846 题内容化导入——「历年真题」笔记本按年份成章、每科一页;选择题题干/选项/答案/解析齐全,大题含子问结构;121 张题图附件化渲染;**答案与解析默认折叠**(spoiler-answer 折叠块,点击显示/收起,内部公式与图片全支持);溯源标签 408真题/科目/年份 + knowledgeBlockIds
- **spoiler 折叠块(M3.5)**:MarkdownPreview 新增 ```spoiler-answer 围栏渲染——收起态铅笔灰「显示答案 ▸」胶囊,展开态虚线框浅底,内部走完整 Markdown 管线;教材包/未来习题类内容可复用
- **导入器图片双通道**:svg 矢量小图内嵌 base64(自包含),png/jpg 栅格大图走附件管线(可去重、页面不膨胀)
- **408 知识包 v1.0.2**:动画播放器按钮交互修复(点击上一步/下一步/重置后立即重绘,移植时遗漏 watch 触发语义);移除页面内可见的 pointId 溯源注释(溯源由 externalId 与 tags 承载)
- **修复三个导入器缺陷**:`parsePack` 入参错误导致所有内容包导入失败(根因);图片相对引用改以 md 文件目录为基准解析(修复深层目录包图片全部丢失);补 `resolve` 导入;动画占位符 ```anim:<id>``` 导入期降级为提示块(M2 换 iframe 内嵌)

### 🐛 v2.8.1 Bug 修复

- **知识库章节折叠黑屏崩溃**:折叠开关的收起态分支渲染 `ChevronRight` 图标未导入,首次点击即 ReferenceError 崩溃整棵组件树(黑屏);补导入后折叠/展开正常
- **知识库性能卡顿**:模块挂载/刷新对每页串行拉取反向链接(77 次 IPC 往返且携带全量正文,实为无消费死代码)已移除;列表查询剥离 content_md/html/annotation 大字段(传输量 2.6MB→约 100KB);`data-imported` 重复刷新去重;搜索结果只回摘录;「导出当前页」改走全量查询防导出空文件;编辑器装载后回种 liveContent 保证大纲可用
- **内容包重导入误跳过**:映射存在但页面已被用户删除时不再按内容哈希判重,存活校验失败视为全新导入重建页面;`getPackState` 同步计入待重建页
- **导入后知识库不刷新**:导入成功派发 `data-imported` 事件;导入器增加预扫描(全部无变化早退不再产生空壳空间)与既有空间/笔记本/章节按名复用(更新落入同一空间)
- **示意图 0×0 塌缩不可见**:源 SVG 根节点 `style=width:100%` 且无宽高属性,`<img>` 内在尺寸失效;导入期剥离根 style 并从 viewBox 注入显式宽高
- **动画不渲染**:rehype-highlight 给代码块加 `hljs` 前缀导致动画 fence 正则锚定失配(渲染成代码块),改非锚定匹配;播放器按钮点击后重绘(移植遗漏 watch 语义)

## v2.7.0

### ✨ 新增功能

- **插件系统一期**(声明式插件,零代码执行):
  - **独立插件模块**(活动栏新入口,对标 VS Code 扩展市场):左侧「已安装 / 市场」标签 + 搜索列表,右侧完整详情页(安装 / 更新 / 启用开关 / 贡献内容 / 卸载)
  - **插件市场**:从 GitHub 仓库(Lousync/Knowbase-plugins)拉取列表,一键安装 / 更新,支持从本地 ZIP 安装;多镜像自动回退(raw → jsDelivr),告别间歇性 502
  - **6 种贡献类型**全部接入:
    - 习惯预设 → 一键导入「习惯打卡」
    - 网址包 → 一键导入「网址导航」(按 URL 自动去重)
    - 主题 → 设置 → 外观 可选,运行时注入 CSS 变量(带消毒防注入)
    - 博客模板 → 写博「模板」弹层虚拟合并(带插件角标,不污染用户模板)
    - 番茄钟预设 → 合并进番茄钟预设按钮组
    - 帮助文档 → 帮助模块对应分类下展示
  - **内容型插件(knowledgePages,A 级)**:插件 zip 内携带结构化 Markdown 页面集合,安装后一键导入为知识库的 空间 → 笔记本 → 章节 → 页面 完整层级
    - 图片自动附件化(png/jpg/webp/svg,含**内联 SVG 提取**与脚本剥离),改写为 attachment:// 引用;`[[双链]]` 原样保留进入关联网络
    - 幂等导入:external_id + 内容哈希判重;本地改过的页面默认跳过,可勾选强制覆盖;「检查更新」识别新增/变化页
    - 导入全程单事务(失败自动回滚),逐页进度条,完成后一键「去知识库查看」
    - 内容型包限额放宽:60MB / 1500 文件 / 单页 512KB
  - **插件分类与合并**:manifest/市场新增 `category` 分类字段,市场侧分类筛选 chips + 条目分类标注;主题贡献支持数组(单插件多主题),4 个独立主题插件合并为「主题合集」单包(主题/场景组合/预设/网址包/模板/文档/示例 七大分类)
  - **编辑器配色跟随主题**:Monaco 编辑器(MD/代码)动态读取当前主题的 CSS 变量生成配色,插件主题下编辑区与界面同色系
  - **主题选择器改版**:卡片网格改为紧凑列表(图标/名称/描述/选中态),支持折叠收起(带数量角标),适配插件主题数量增长
  - **插件图标**:每个插件可携带独立图标(manifest `icon` 字段,安装时校验),展示于工具箱卡片、插件列表与详情、市场;市场支持 `iconUrl` 安装前预览;缺失时回退拼图占位
  - **内置 UI 插件**:强密码生成器迁移为首个内置插件(独立设计图标,随应用分发,支持版本升级自动覆盖,不可卸载可禁用)
  - 禁用 / 卸载插件后,所有挂载点内容即时消失;已导入的预设与数据不受影响
- 插件仓库与分发:`Lousync/Knowbase-plugins`(registry.json 索引 + 插件 zip 直链),应用内 10 分钟缓存 + 多镜像
- **首批官方插件上架**(12 个,全部独立功能图标):
  - 场景组合包:考研 408 预备包(习惯 + 网址 + 模板三合一,A 级)
  - 主题系列:GitHub Dark / GitHub Light / 护眼米白纸感 / 赛博朋克夜城(S 级)
  - 番茄钟进阶预设:52-17 法 / 90min 深潜 / 考试模拟(S 级)
  - 网址包系列:C++ 官方文档集 / AI 工具导航 / 算法刷题站合集(A 级)
  - 博客模板精选:周复盘 / 读书笔记 / 错题本(S 级)
  - 帮助文档扩展:Git 工作流速查 / Markdown 进阶语法(S 级)
  - 知识包:Git 入门教程包(内容型示例,含双链/标签/SVG 示意图,A 级)

### 🔒 安全设计

- 插件为纯数据包(manifest + 资源),**不执行任何插件代码**;`type: code` 在清单校验阶段即被拒绝
- 下载域名白名单(仅 GitHub 与 jsDelivr 镜像域)、解压逐条目 Zip Slip 防护、manifest 严格校验(id 格式 / 贡献类型白名单 / 体积 20MB 与文件数 500 上限 / engineVersion 兼容检查)、卸载仅限插件根目录
- 插件主题 CSS 变量名 / 值白名单消毒;主题 id 类名消毒(修复含点号 id 导致主题无法生效的问题)

## v2.6.4

## v2.6.4

### 🐛 Bug 修复

- 修复开发模式启动失败（`EACCES: permission denied 127.0.0.1:6173`）：dev server 端口与 Windows Hyper-V/winnat 动态排除端口段冲突，迁移至 7173
- 修复发布版每次启动都弹出新手引导的问题：设置异步加载与首帧判断存在竞态，引导在真实设置（`onboardingDone`）到达前误判为首次启动；现设置上下文新增 `ready` 标志，新手引导、启动页选择与初始设置应用均等待设置加载完成后执行

## v2.6.3

### ✨ 新增功能

- **帮助模块反馈入口**：侧栏底部新增「反馈问题」按钮，一键跳转 GitHub 仓库 Issues 页面提交问题与建议
- **检查更新**（设置 → 高级 → 关于与更新）：显示当前版本，一键检查 GitHub Releases 上的新版本——发现新版本时展示更新内容（Markdown 渲染发布说明）并支持应用内直接下载安装包（实时进度），下载完成一键运行安装向导；也可随时跳转发布页手动下载

## v2.6.2

### 🔒 安全修复（全项目漏洞探查）

> 经三路源码审计（Electron 主进程 / 渲染进程注入面 / 数据层业务逻辑）后集中修复，完整报告见 `.claude/plans/security-audit-2026-08.md`

#### 高危

- **修复备份 ZIP 导入的路径穿越（Zip Slip）**：恶意备份包可经 `export.json` 中的附件路径把任意文件写到启动目录等任意位置，或借 `attachment://` 协议读取任意本地文件；现在所有附件路径必须解析回附件目录内，越界一律跳过
- **修复链接打开链路无协议白名单**：书签/笔记中的 `file://`、UNC、自定义协议链接可拉起外部程序执行；现在书签仅放行 http(s)，本地路径仅允许打开应用数据目录内的文件；顺带修复博客中 https 链接无法在浏览器打开的问题
- **修复数据库整体替换导入无预检**：选到损坏/伪造的 .db 会直接覆盖唯一数据库且不可逆；现在先在内存中试开验证，替换前自动备份当前库，失败自动还原
- **修复数据库写盘非原子**：写入途中断电/崩溃会损坏整个数据库且无法启动；现在改为临时文件 + 原子改名替换，自动轮转 `.bak` 备份，启动时主库损坏自动回退并留存损坏现场

#### 中危

- 修复「清空所有数据」遗漏 `knowledge_manual_links` 表导致手动关联残留
- 数据库迁移全程包进事务，杜绝"已改表但未记标记"的中间态；041 迁移补幂等保护
- 6 处动态 UPDATE 的列名拼接增加白名单校验，封堵渲染层注入 SQL 片段的可能
- 数据导入全程事务化，失败自动回滚不再留下"半更新"数据；备份导入改为业务数据成功后才动附件与用户资料
- 回收站恢复一致性：分类/相册已删除时兜底置空不再卡死条目、同日期日志拒绝恢复避免重复、说说先插记录后恢复附件；单条损坏不再拖垮整个回收站列表
- 渲染层加固：主窗口与密码悬浮窗开启 sandbox 沙箱；应用窗口禁止任何导航与 `window.open` 打开本地文件
- IPC 面收敛：导出写入仅接受"保存对话框授权过的路径"；附件 owner 参数白名单校验；设置写入仅接受已知键与匹配类型
- **密码本加密存储**：密码列改用系统级加密（Windows DPAPI），存量明文密码启动时自动加密；备份导出仍为明文（供迁移）；删除进回收站的快照同步加密；复制密码 30 秒后仅当剪贴板仍是该密码时才清空（不再覆盖用户后续复制的内容）；密码快速填充悬浮窗复制同样增加自动清理；从回收站导出密码条目到 Markdown 时不再写入明文密码
- 时区统一：周月总结统计、连续写作天数、状态栏日期等全部改用本地时区，修复东八区凌晨数据算错天的问题

#### 其他

- 回收站保留天数设置非法值兜底（NaN 不再导致回收站报错）

## v2.6.1

### ✨ 新增功能

- **新用户引导**：首次启动（含老版本升级后首次）弹出五步引导向导——欢迎、六大模块速览、个性化快设（主题 / 启动页 / 打卡提醒即点即生效）、数据与安全说明、快捷键速查；完成或跳过后不再弹出，设置 → 高级 可随时重新查看
- **工具箱帮助文档**：帮助模块新增「工具箱」分类，为全部 7 个工具（体重追踪、密码本、强密码生成器、网址导航、番茄钟、习惯打卡、远程监督）各撰写一篇与实际功能逐一核对的帮助文档
- 帮助文档文件名全面中文化（博客写作、键盘快捷键、常见问题等），侧栏辨识更直观

### 🐛 Bug 修复

- **修复全局快捷键跨模块串扰**：模块首次访问后会驻留后台（仅隐藏不卸载），其全局键盘监听仍会响应——例如在日志模块按 `Ctrl+N` 会弹出新建任务、在任务模块按 `Ctrl+S` 触发日志保存；现在所有模块级快捷键（日志 / 任务 / 知识库的 Ctrl+N、Ctrl+S、Ctrl+W、Ctrl+Tab、F2、Delete、Esc 等）只对当前激活模块生效
- 修复帮助文档 frontmatter 因 CRLF 换行符导致分类 / 图标解析始终失败的问题

### 🔧 功能优化

- 工具箱卡片视觉升级：图标增大至 26px、卡片内边距与圆角加大、网格加宽；移除卡片说明小字与标题副标题，更简洁聚焦
- 知识库中心空态与导出模块侧栏移除冗余说明文字

## v2.6.0

### ✨ 新增功能

- **知识库网络化一期**：搜索结果正文命中摘录与关键词高亮（多词 AND）；`[[空链接]]` 虚线样式点击一键建页；反链面板升级为带引用上下文摘录的「关联网络」；页面间手动关联（双向展示、相关性推荐含理由标注）；PDF / 代码 / XMind 页面通用注解层，注解中的双链同样入图
- **沉浸阅读模式**：Ctrl+Shift+R 或工具栏 📖 进入，隐藏全部界面控件，720px 居中排版，Esc / 顶部悬停退出；阅读中可循双链连续跳转
- **新建页面先命名**：所有入口（目录悬浮 + / 右键菜单 / 章节面板 / Ctrl+N）统一为先输入名称再创建，与笔记本、目录体验一致
- **右栏关联面板 ResizablePanel 化**：支持拖拽调宽、拖过半程吸边收起、从边缘拖出展开（收起态避开系统窗口缩放热区）
- **周/月总结面板**（每日博客）：可自定义总结日（周几 / 每月第一天 / 最后一天 / 固定日），命中日期的博文文末自动附上与正文排版融合的总结节——区间统计（坚持打卡、博客文章、新建知识页、番茄钟专注时长、完成任务数）+ 下期任务快速创建
- **下期任务联动日程模块**：总结面板里只记大概，回车即创建；任务自动带「周期任务」标签并落在总结日次日，时间 / 四象限 / 子任务等细节到日程模块继续编辑
- **番茄钟专注落库**：自然完成的专注场次写入数据库，为统计提供数据支撑（此前纯内存不记录）
- **博客模板**：设置 → 博客 中自由创建 / 编辑 / 删除模板；写博时工具栏「模板」一键套用——空文档直接填入，已有内容追加到文末
- **打卡提醒**：设置 → 提醒 中配置开关与时间；到点若当天仍有计划内习惯未打卡则弹窗提醒（按各自周期规则判定，每天至多一次，打开应用过晚会补提醒）
- **说说时间线可见性**：向相册添加照片时可选择"仅存相册"不上时间线；已发布的说说可在相册卡片或时间线随时切换显示状态（带「仅相册」角标）
- **导出导入增强**：备份包新增打卡（习惯 + 记录）与网址导航（分类 + 书签）两类数据，按模块勾选导出、合并导入去重
- **SVG 图片支持**：Markdown 渲染、图片上传选择、剪贴板复制全链路支持 SVG（此前因 MIME 类型缺失无法显示）
- 帮助文档新增「周月总结与模板」篇

### 🐛 Bug 修复

- 修复「清空所有数据」遗漏相册（moments_albums）、密码本、体重记录等表导致的数据残留问题
- 修复活动栏拖拽处理器的潜在空指针问题

## v2.5.0

### ✨ 新增功能

- **远程监督工具**（工具箱 → 效率工具）：打卡情况通过 Webhook 实时推送给远程监督者（家长 / 学习伙伴），监督者无需安装本应用，在微信 / 钉钉群内即可查看
- 四平台推送适配器：Server酱（微信）、企业微信群机器人、钉钉群机器人（支持加签密钥）、自定义 Webhook（POST JSON），各平台附监督者侧配置引导说明
- 打卡即时推送：习惯打卡成功后立即通知监督者，附日期与连续打卡天数；取消打卡不推送，推送失败不影响打卡本身
- 每日汇总：到达设定时间自动推送当天全部习惯完成清单（✅ / ⬜ 与完成率）；应用启动时自动补发隔夜漏发，每天自动只尝试一次防轰炸
- 免打扰时段：区间内推送挂起待发，结束后自动补发（支持 23:00~07:00 跨天区间）
- 推送历史页：全部推送记录状态跟踪（成功 / 失败 / 挂起）与真实本地时间展示；失败项显示错误原因，支持单条重推、批量补推失败、一键清空历史（两步确认防误触）
- 发送测试消息：保存配置后即可验证 Webhook 连通性；「立即发送今日汇总」支持手动随时触发
- 平台地址隔离：切换推送平台时各自填写的 SendKey / Webhook 地址互不串显、互不覆盖

### 🐛 Bug 修复

- 修复「清空所有数据」未清除习惯打卡、网址导航数据表的问题（现一并纳入清理范围）

## v2.4.0

### ✨ 新增功能

- **网址导航工具**（工具箱 → 数据工具）：学习资料网址集中管理，自定义分类（名称+颜色）、实时搜索、域名首字母彩色头像卡片、点击/按钮一键跳转默认浏览器
- 网址导航导出：JSON 完整备份（可再导入合并）+ HTML Netscape 书签格式（可直接导入 Chrome / Edge / Firefox 书签栏）
- 网址导航导入：JSON 文件按分类名自动合并，同 URL 自动去重，Toast 汇总导入结果
- **开发版数据隔离**：`npm run dev` 使用独立 `%APPDATA%/knowbase (dev)` 目录（数据库/附件/设置全隔离），首次运行自动从正式版快照迁移；开发版与已安装版可同时运行互不抢锁
- 标题栏新增 DEV 角标，直观区分开发窗口与正式窗口

## v2.3.0

### ✨ 新增功能

- **习惯打卡工具**（工具箱 → 效率工具）：自定义习惯与三种周期规则（每天 / 每周指定星期几 / 每周 N 次灵活制），一键纯勾选打卡
- 打卡「今天」视图：进度条统计、已完成置灰沉底、灵活制显示本周进度
- 打卡「日历」视图：月历聚合各习惯色点，点选日期查看明细并支持任意历史日期补卡 / 撤销
- 打卡「统计」视图：当前连续、最长连续、累计次数、近 30 天完成率
- 正向反馈：完成打卡时打勾圈位置彩纸迸发 + 圆圈弹跳动画；今日全部完成全屏三连发庆祝；连续天数达里程碑弹提示

### ♻️ 移除

- 移除 AI 问答模块及全部关联链路（工具面板、设置分区、主进程处理器、IPC、相关配置项）
- 移除工具箱未实现的占位卡片（JSON 格式化 / Base64 编解码 / 时间戳转换 / 单位换算）

## v2.2.0

### ✨ 新增功能

- **知识库空间（工作区）**：顶级空间分组容器，根层级仅允许空间；打开空间后进入沉浸视图，其他空间不可见
- **内置 PDF 阅读器**：基于 pdf.js 在应用内渲染 PDF，支持翻页、缩放、适合宽度、Ctrl+滚轮缩放
- **PDF 阅读方式切换**：内置阅读 / 本地工具打开，选择持久化记忆

### 🔧 功能优化

- 空间内右键空白处可直接新建页面 / 目录 / 笔记本（落点为当前空间）
- 页面 / 目录支持拖拽进空间；空白处拖入 = 放入当前空间
- 跨空间移动：右键「移到目录...」可选目标空间；粘贴支持跨空间
- 移除编辑器顶部标题行，页面改名统一在侧边栏进行（右键「重命名」或 F2 内联改名）
- 空间列表层顶部居中「工作区」标题；文件/大纲切换仅在空间内显示并移至返回栏下方
- 空间行移除冗余展开箭头与 hover 创建按钮

### 🐛 Bug 修复

- 回收站：删除内容后切到回收站立即显示（模块激活时自动刷新）
- 回收站：恢复内容后知识库原位置立即可见（监听 recycle-restored 事件刷新）
- 修复空间内右键无法创建目录/笔记本的问题
- 兼容旧版无 attachment_id 的 PDF 附件读取（按附件文件名回退）

## v2.1.0

### ✨ 新增功能

- **博客/知识库正文图片**：编辑器支持粘贴截图、拖拽图片文件、工具栏「图片」按钮插入图片；HEIC/HEIF 自动转 JPEG；正文内联渲染
- **图片内外剪贴板互通**：说说灯箱、博客/知识库预览图片一键复制到系统剪贴板；说说编辑器 Ctrl+V 粘贴截图

### 🔧 功能优化

- **说说相册分组折叠卡片**：相册内照片按说说分组，折叠卡片点击展开查看文字与照片
- **相册双布局切换**：列表（矩形）/ 网格（正方形）两种排列风格
- **相册卡片展开动画**：展开/收起平滑过渡

### 🐛 Bug 修复

- 备份包导出遗漏博客正文内联图片（blog_entry 附件）
- 复制图片到剪贴板在含中文路径下失败

## v2.0.1

### 🔧 功能优化

- 知识库侧栏整合为「文件 / 大纲」两个标签页，移除旧的大纲切换逻辑
- 右键菜单边界定位，靠近窗口边缘时自动保持可见

## v2.0.0

### ✨ 新增功能

- **统一附件子系统**：图片从 base64 改为文件化落盘，说说/知识库附件/头像统一管理 + 孤儿清理
- **备份包导出/导入**：单一 ZIP 备份包（JSON 元数据 + attachments 目录），完整还原 + 拖拽导入
- **HEIC/HEIF 转 JPEG**：说说/相册导入 HEIC/HEIF 本地转码
- **知识库大纲标题搜索**：顶栏搜索支持大纲标题，页面顶部 UI 整合，文件类型识别调整
- **导出收敛为单一备份包格式**

## v1.8.0

### ✨ 新增功能

- **说说模块**：单机时间线 + 相册归档 + 标签搜索 + 九宫格图片 + 灯箱
- 相册系统：直接添加照片、手动设置封面、跨说说连续翻页

### 🔧 功能优化

- 说说卡片多轮 UI 迭代（封面、九宫格、文案折叠、个人资料卡片收缩）

## v1.7.5

### ✨ 新增功能

- **体重追踪器**：Canvas 折线图多系列对比、鼠标滚轮缩放、点击数据点操作

## v1.7.4

### ✨ 新增功能

- **密码本悬浮窗自动填充**（Ctrl+Alt+P）

### 🔧 功能优化

- 密码填充改为手动复制模式

## v1.7.3

### ✨ 新增功能

- **密码本工具** + 密码本导入/导出

### 🔧 功能优化

- 密码本删除条目移入回收站

## v1.7.2

### ✨ 新增功能

- 博客月份切换器 + 全部文章视图 + 标签快捷筛选
- 知识库导入整个文件夹（递归创建目录结构+页面）
- 知识库空白处右键新建页面/目录/笔记本

### 🐛 Bug 修复

- 已保存文件仍弹未保存确认、删除页面弹确认、Ctrl+S 指示器同步等
- 目录拖入笔记本章节导致目录消失的问题

## v1.7.1

### ✨ 新增功能

- **XMind 思维导图支持**：知识库可导入 `.xmind` 文件，与 PDF 相同模式——文件存入本地附件目录，点击按钮调用系统 XMind 软件打开编辑

### 🔧 功能优化

- **VS Code 风格预览标签页**：点击文件打开时，如果当前标签页未被修改，新文件会替换当前标签而不是堆积。只有修改后的文件才会固定留存。预览标签以斜体显示
- **搜索框简化**：placeholder 改为"搜索"，移除 Ctrl+P 快捷键提示标签
- **侧边栏行为修复**：关闭所有文件标签页后不再强制打开第一侧边栏

## v1.7.0

### ✨ 新增功能

- **知识库搜索栏**：顶栏居中搜索框，类似 VS Code 风格。支持 Ctrl+P 快捷键唤出，实时模糊匹配——输入关键词同时搜索页面标题、目录名、笔记本名和标签名。搜索结果下拉面板用 ↑↓ 选择、Enter 打开、Esc 关闭。匹配到页面直接打开，匹配到目录/笔记本自动在资源管理器树中展开定位，匹配到标签可展开查看该标签下的所有页面
- **页面标签系统**：页面编辑器工具栏下方新增标签栏，可为页面添加/删除彩色标签（最多 5 个），标签随自动保存持久化
- **右键菜单增强**：文件和目录的右键菜单新增"重命名"和"删除"按钮，"删除"以红色标注。粘贴按钮在剪贴板为空时显示为灰色不可点击状态

### 🐛 Bug 修复

- **同类型文件图标不一致**：手动创建的页面与导入的同类型文件显示不同图标（如 `.cpp` 和空文件类型），现已统一规范化

### 🔧 功能优化

- **右键菜单复制/剪切/粘贴**：知识库内支持复制、剪切页面或目录，在空白处或目标目录上粘贴。复制创建副本，剪切则移动到新位置。剪切中的项目半透明显示。支持 Ctrl+C/X/V 快捷键

## v1.6.1-programmer.1

### 🐛 Bug 修复

- **知识库页面/分类拖拽移动后 sort_order 不重置**：移动页面或分类到新父级时，`sort_order` 保留原值导致位置不可预期。数据库层 `updatePage`/`updateCategory` 现在检测到 `categoryId`/`parentId` 变化时自动将 `sort_order` 置为 `MAX+1`（追加到末尾）
- **ChapterPanel 页面列表不支持拖拽排序**：章节内的页面只能通过上/下箭头按钮逐位移动，现在支持拖拽到目标页面的上半部分（插入之前）或下半部分（插入之后），新增 `knowledge:reorderPage` IPC handler 按索引精确重排
- **拖拽页面到笔记本时使用过期分类数据**：`handleDropOnNotebook` 从组件闭包读取 `categories` 可能已过期，改用 `getKnowledgeCategories()` 实时获取最新数据

### ✨ 功能优化

- **分类拖拽互操作（3种）**：① 目录可拖入另一目录成为子目录；② 无子目录的文件夹可拖入笔记本成为章节；③ 笔记本可拖入目录成为子节点。ChapterPanel（第二侧边栏）章节行支持接受分类拖放，页面区域空处支持分类拖放（变为笔记本章节）
- **分类拖拽/右键移动彻底修复**：三个根因——① `onDragOver` 依赖 `dataTransfer.getData()`，Chromium 安全策略在 dragover 中可能返回空→改用 `useRef` 在 dragStart 时记录拖曳数据；② 循环检测方向相反：`isDescendant(target, dragged)` 应改为 `isDescendant(dragged, target)` 防止父目录拖入子目录；③ 移动成功后目标父目录未展开→目录"消失"→现在自动展开
- **修复展开目录的拖放盲区**：之前展开目录的子内容区域（子页面+子分类占大面积）缺少 `onDragOver`/`onDrop`，拖放入此区域被静默吞掉。现在展开容器承接所有拖放事件并 delegate 到父分类，树容器 fallback 也改为主动 delegate
- **博客模块 Markdown 大纲**：OutlinePanel + parseHeadings 从 knowledge 移到 `shared/components/`，博客编辑器新增大纲切换按钮（工具栏 `ListTree` 图标），Ctrl+O 快捷键，点击大纲标题跳转到对应行

## v1.6.0-programmer.1

### 🐛 Bug 修复

- **零碎任务不再出现在四象限图中**：`daily` 任务（零碎任务）无象限属性，从 `QuadrantChart` 传入数据中过滤掉，避免其随机落入某个象限格
- **博客阅读模式长文章可滚动**：`<main>` 容器缺少 `flex flex-col` 导致子元素 `EntryDetail` 的 `flex-1 overflow-y-auto` 无高度参照，现已修复

### ✨ 功能优化

- **知识库大纲实时跟随编辑器内容**：大纲面板不再等待自动保存（2s 防抖），而是在 Monaco 编辑器每次输入时即时刷新标题层级树
- **大纲按钮整合至编辑器工具栏**：零散页面（非笔记本页面）没有第二侧边栏也能点击编辑器顶部工具栏的 `<ListTree>` 按钮唤出大纲面板。第二侧边栏的原大纲按钮已移除，避免重复入口
- **大纲面板空白保护**：切换页面时重置实时内容缓存，避免短暂闪现上一页的大纲数据
- **知识库跨模块页面保活**：从知识库切换到其他模块后再返回，已打开的页面和编辑器状态不再丢失。所有 Tab 模块首次挂载后保持常驻，通过 `display:none` 隐藏而非销毁
- **折叠侧边栏拖拽拉出**：侧边栏折叠后，鼠标悬停边缘显示蓝色分割条（`var(--accent)`），拖拽超过 `minWidth/2` 即可重新拉出侧边栏。行为对标 VS Code 的侧边栏折叠/展开交互

### 📝 技术细节

| 文件 | 改动 |
|------|------|
| `src/modules/schedule/index.tsx` | `QuadrantChart` 接收 `pendingTodos.filter(t => t.taskType !== 'daily')` |
| `src/modules/blog/index.tsx` | `<main className="flex-1 overflow-hidden">` → `flex flex-col` |
| `src/modules/knowledge/index.tsx` | 新增 `liveContent` 状态 + `outlineHeadings` 使用实时内容；`PageEditor` 传入 `onContentChange`/`onToggleOutline` |
| `src/modules/knowledge/components/PageEditor.tsx` | 新增 `onContentChange` / `onToggleOutline` prop；Monaco `onChange` 回调；工具栏加大纲按钮 |
| `src/modules/knowledge/components/ChapterPanel.tsx` | 移除第二侧边栏大纲按钮、`ListTree` 图标导入、`onToggleOutline` prop |
| `src/App.tsx` | 新增 `mountedTabs` ref + `renderTab()` 辅助函数，所有 Tab 模块首次访问后保持挂载（`display:none` 隐藏而非销毁）；`main` 加 `flex flex-col`；传递 `onSnapOpenSidebar` |
| `src/components/shared/ResizablePanel.tsx` | 新增 `onSnapOpen` prop + 折叠边缘分割条：`visible=false` 时显示 4px 蓝色悬停边缘，拖拽超 `minWidth/2` 触发 `onSnapOpen` |
| `src/modules/knowledge/components/ChapterPanel.tsx` | 移除第二侧边栏大纲按钮、`ListTree`、`onToggleOutline` prop |
| `src/modules/blog/index.tsx` | 新增 `onSnapOpenSidebar` prop → `ResizablePanel` |
| `src/modules/schedule/index.tsx` | 同上 |
| `src/modules/knowledge/index.tsx` | 同上；L1 `onSnapOpen` 同时恢复 `showCategoryPanel` + `onSnapOpenSidebar`；L2 `onSnapOpen` 仅在有选中笔记本时启用 |
| `CHANGELOG.md` | 新建（不纳入 git 跟踪，仅用于 GitHub Release 发布） |
